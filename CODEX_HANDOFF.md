# Project Overview
- Project: `futures_intraday_model`; purpose: intraday futures research pipeline using Databento continuous-contract 1-minute OHLCV data; research and walk-forward validation only, not live-trading or production readiness. Project outline authority: use `PROJECT_OUTLINE.md`; Repo guidance authority: use `AGENTS.md`; Handoff role: this file is mutable cross-run state; Solo Codex files: keep `AGENTS.md`, `PROJECT_OUTLINE.md`, and `CODEX_HANDOFF.md` only.
# Current State
- 2026-07-10 Master Audit terminal closeout refresh implemented and executed report-only. Added `scripts/validation/build_master_audit_terminal_closeout_refresh.py` and `tests/validation/test_build_master_audit_terminal_closeout_refresh.py`; updated `PROJECT_OUTLINE.md` with the command. Generated evidence: `reports/master_audit/master_audit_terminal_closeout_refresh_20260710/master_audit_terminal_closeout_refresh.{json,md}` with `PASS_MASTER_AUDIT_TERMINAL_CLOSEOUT_REFRESH_REPORT_ONLY`, failure_count 0, decision `TERMINAL_CLOSEOUT_CURRENT_LINE_CLOSED_BLOCKED_REPORT_ONLY`, `data_integrity=PASS`, `statistical_validity=FAIL`, `operational_resilience=FAIL`, `full_master_audit_accepted=false`, `model_trust_ready=false`, `promotion_allowed=false`, artifact-freeze false, final-holdout false, paper/live false, and production false. Validation passed: focused pytest `8 passed`; approved regression group `36 passed`; live report command PASS with exactly the JSON/MD pair. No ledgers, registry, trial statuses, source reports, experiment ledger rows, prediction parquet, model artifacts, Phase 8 outputs, gap maps, staging, commits, or pushes were mutated; no WFA/modeling, predictions generation, Phase 8 refresh, provider/network calls, promotion, artifact freeze, final holdout, paper/live, cleanup, or git publication ran.
- 2026-07-10 Master Audit statistical-validity closeout boundary decision implemented and executed report-only. Added `scripts/validation/build_master_audit_statistical_validity_closeout_boundary_decision.py` and `tests/validation/test_build_master_audit_statistical_validity_closeout_boundary_decision.py`; updated `PROJECT_OUTLINE.md` with the command. Generated evidence: `reports/master_audit/master_audit_statistical_validity_closeout_boundary_decision_20260710/master_audit_statistical_validity_closeout_boundary_decision.{json,md}` with `PASS_MASTER_AUDIT_STATISTICAL_VALIDITY_CLOSEOUT_BOUNDARY_DECISION_REPORT_ONLY`, failure_count 0, decision `PERMANENTLY_BLOCK_CURRENT_LINE_STATISTICAL_VALIDITY_GAP_FROM_EXISTING_EVIDENCE`, `statistical_validity=FAIL`, `statistical_validity_ready=false`, `master_audit_statistical_validity_accepted=false`, `gap_map_refresh_allowed=false`, `statistical_validity_gap_upgrade_allowed=false`, `model_trust_ready=false`, `promotion_allowed=false`, paper/live false, and production false. Validation passed: focused pytest `7 passed`; approved regression group `33 passed`; live report command PASS with exactly the JSON/MD pair. No ledgers, registry, trial statuses, source reports, experiment ledger rows, prediction parquet, model artifacts, Phase 8 outputs, gap maps, staging, commits, or pushes were mutated; no WFA/modeling, predictions generation, Phase 8 refresh, provider/network calls, promotion, artifact freeze, final holdout, paper/live, cleanup, or git publication ran.
- 2026-07-10 Master Audit statistical-validity acceptance-boundary decision implemented and executed report-only. Added `scripts/validation/build_master_audit_statistical_validity_acceptance_boundary_decision.py` and `tests/validation/test_build_master_audit_statistical_validity_acceptance_boundary_decision.py`; updated `PROJECT_OUTLINE.md` with the command. Generated evidence: `reports/master_audit/master_audit_statistical_validity_acceptance_boundary_decision_20260710/master_audit_statistical_validity_acceptance_boundary_decision.{json,md}` with `PASS_MASTER_AUDIT_STATISTICAL_VALIDITY_ACCEPTANCE_BOUNDARY_DECISION_REPORT_ONLY`, failure_count 0, decision `BLOCK_STATISTICAL_VALIDITY_ACCEPTANCE_CURRENT_LINE_TERMINAL_FAILS_AND_MISSING_NULL_REGIME_EVIDENCE`, `remaining_statistical_validity_blockers_actionable_for_current_line=false`, `new_generated_null_regime_or_variant_evidence_required_for_any_future_reconsideration=true`, PSR `FAIL` with probabilistic_sharpe 0.0 / z_score -18.2206357418968, PBO `FAIL_NO_VARIANT_PERFORMANCE_MATRIX`, Deflated Sharpe `FAIL_NEGATIVE_SHARPE_AFTER_TRIAL_COUNT_DEFLATION`, multiple-testing `FAIL_BONFERRONI_ADJUSTED_PSR`, regime `FAIL`, random-entry/null terminal fail, 2x cost stress terminal fail, label-shuffle/timing-shift missing, `statistical_validity_ready=false`, `master_audit_statistical_validity_accepted=false`, `model_trust_ready=false`, `promotion_allowed=false`, paper/live false, and production false. Validation passed: focused pytest `8 passed`; approved regression group `31 passed`; live report command PASS with exactly the JSON/MD pair. No ledgers, registry, trial statuses, source reports, experiment ledger rows, prediction parquet, model artifacts, Phase 8 outputs, gap maps, staging, commits, or pushes were mutated; no WFA/modeling, predictions generation, Phase 8 refresh, provider/network calls, promotion, artifact freeze, final holdout, paper/live, cleanup, or git publication ran.
- 2026-07-10 Master Audit statistical-validity regime/adversarial disposition implemented and executed report-only. Added `scripts/validation/build_master_audit_statistical_validity_regime_adversarial_disposition.py` and `tests/validation/test_build_master_audit_statistical_validity_regime_adversarial_disposition.py`; updated `PROJECT_OUTLINE.md` with the command. Generated evidence: `reports/master_audit/master_audit_statistical_validity_regime_adversarial_disposition_20260710/master_audit_statistical_validity_regime_adversarial_disposition.{json,md}` with `PASS_MASTER_AUDIT_STATISTICAL_VALIDITY_REGIME_ADVERSARIAL_DISPOSITION_REPORT_ONLY`, failure_count 0, random-entry/null disposition `TERMINAL_FAIL_CANDIDATE_DOES_NOT_BEAT_RANDOM_MEDIAN`, 2x cost stress disposition `TERMINAL_FAIL_EDGE_DOES_NOT_SURVIVE_2X_COST`, label-shuffle disposition `MISSING_REQUIRED_EVIDENCE_SEPARATE_BOUNDED_SHUFFLE_HARNESS_REQUIRED`, timing-shift disposition `MISSING_REQUIRED_EVIDENCE_SEPARATE_BOUNDED_TIMING_SHIFT_HARNESS_REQUIRED`, regime disposition `FAIL_MISSING_EXPLICIT_CAUSAL_REGIME_EVIDENCE_NOT_ACTIONABLE_FOR_CURRENT_LINE`, PSR `FAIL`, regime `FAIL`, `statistical_validity_ready=false`, `master_audit_statistical_validity_accepted=false`, `model_trust_ready=false`, `promotion_allowed=false`, paper/live false, and production false. Validation passed: focused pytest `9 passed`; approved regression group `36 passed`; live report command PASS with exactly the JSON/MD pair. No ledgers, registry, trial statuses, source reports, experiment ledger rows, prediction parquet, model artifacts, Phase 8 outputs, gap maps, staging, commits, or pushes were mutated; no WFA/modeling, predictions generation, Phase 8 refresh, provider/network calls, promotion, artifact freeze, final holdout, paper/live, cleanup, or git publication ran.
- 2026-07-10 Master Audit post-recompute statistical-validity reconciliation implemented and executed report-only. Added `scripts/validation/build_master_audit_post_recompute_statistical_validity_reconciliation.py` and `tests/validation/test_build_master_audit_post_recompute_statistical_validity_reconciliation.py`; updated `PROJECT_OUTLINE.md` with the command. Generated evidence: `reports/master_audit/master_audit_post_recompute_statistical_validity_reconciliation_20260710/master_audit_post_recompute_statistical_validity_reconciliation.{json,md}` with `PASS_MASTER_AUDIT_POST_RECOMPUTE_STATISTICAL_VALIDITY_RECONCILIATION_REPORT_ONLY`, failure_count 0, PBO `FAIL_NO_VARIANT_PERFORMANCE_MATRIX`, Deflated Sharpe `FAIL_NEGATIVE_SHARPE_AFTER_TRIAL_COUNT_DEFLATION`, multiple-testing `FAIL_BONFERRONI_ADJUSTED_PSR`, trial/search/multiple-testing family counts `22/10/2`, PSR `FAIL`, regime `FAIL`, bootstrap CI `PASS`, parameter stability `PASS`, `missing_trial_log_blockers_replaced=true`, `statistical_validity_ready=false`, `master_audit_statistical_validity_accepted=false`, `model_trust_ready=false`, `promotion_allowed=false`, paper/live false, and production false. Validation passed: focused pytest `9 passed`; approved regression group `31 passed`; live report command PASS with exactly the JSON/MD pair. No ledgers, registry, trial statuses, source reports, experiment ledger rows, prediction parquet, model artifacts, Phase 8 outputs, gap maps, staging, commits, or pushes were mutated; no WFA/modeling, predictions generation, Phase 8 refresh, provider/network calls, promotion, artifact freeze, final holdout, paper/live, cleanup, or git publication ran.
- 2026-07-10 Phase 9 trial-search statistical-validity recompute path implemented and executed bounded. Updated `scripts/phase9_research/statistical_validity.py` with `--trial-search-ledger`, added tests in `tests/phase9_research/test_statistical_validity.py`, and updated `PROJECT_OUTLINE.md` with the executable command. Generated evidence root: `reports/statistical_validity/tier1_core_phase6_full_predictions_20260706_trial_search_recompute_20260710/` with exactly `statistical_validity_summary.json`, `bootstrap_confidence_intervals.csv`, `stability_matrix.csv`, `adversarial_tests.csv`, and `statistical_validity.md`. A first run using a new `--run` id correctly failed the stop condition due prediction-manifest run mismatch and was overwritten inside the same approved output root with the manifest run id `tier1_core_phase6_full_predictions_20260706`; final recompute status is expected `FAIL`, `prediction_count=9172416`, `policy_trade_count=1347`, `failure_count=5`, `trial_count=22`, `search_family_count=10`, `multiple_testing_family_count=2`, `pbo=FAIL_NO_VARIANT_PERFORMANCE_MATRIX`, `deflated_sharpe=FAIL_NEGATIVE_SHARPE_AFTER_TRIAL_COUNT_DEFLATION`, `multiple_testing_adjustment=FAIL_BONFERRONI_ADJUSTED_PSR`, `probabilistic_sharpe=FAIL`, `bootstrap_confidence_intervals=PASS`, `parameter_stability=PASS`, `regime_breakdowns=FAIL`, two-times cost stress `FAIL`, random-entry and label-shuffle remain `MISSING_WITH_REASON`, `statistical_validity_ready=false`, and `model_promotion_allowed=false`. Validation passed: focused Phase 9 pytest `5 passed`; approved regression group `22 passed`. No ledgers, registry, trial statuses, source reports, experiment ledger rows, prediction parquet, model artifacts, Phase 8 outputs, gap maps, Master Audit reports, staging, commits, or pushes were mutated; no WFA/modeling, predictions generation, Phase 8 refresh, provider/network calls, promotion, artifact freeze, final holdout, paper/live, cleanup, or git publication ran.
- 2026-07-10 Master Audit statistical-validity recompute decision implemented and executed report-only. Added `scripts/validation/build_master_audit_statistical_validity_recompute_decision.py` and `tests/validation/test_build_master_audit_statistical_validity_recompute_decision.py`; updated `PROJECT_OUTLINE.md` with the command. Generated evidence: `reports/master_audit/master_audit_statistical_validity_recompute_decision_20260710/master_audit_statistical_validity_recompute_decision.{json,md}` with `PASS_MASTER_AUDIT_STATISTICAL_VALIDITY_RECOMPUTE_DECISION_REPORT_ONLY`, failure_count 0, PBO/Deflated Sharpe/multiple-testing recompute decisions `READY_FOR_SEPARATE_BOUNDED_STATISTICAL_RECOMPUTE`, trial count 22, search families 10, multiple-testing families 2, `statistical_recompute_executed=false`, `statistical_validity_ready=false`, `model_trust_ready=false`, `promotion_allowed=false`, paper/live false, and production false. It preserved current PSR/regime/adversarial blockers and did not run `scripts.phase9_research.statistical_validity` or read/write prediction data. Validation passed: focused pytest `9 passed`; approved regression group `30 passed`; live report command PASS with exactly the JSON/MD pair. No ledgers, registry, trial statuses, source reports, experiment ledger rows, data/model artifacts, Phase 8 outputs, gap maps, staging, commits, or pushes were mutated in this slice; no data/model commands, WFA/modeling, predictions, Phase 8 refresh, provider/network calls, promotion, artifact freeze, final holdout, paper/live, or cleanup ran.
- 2026-07-10 Master Audit post-mutation trial/search completeness reconciliation implemented and executed report-only. Added `scripts/validation/build_master_audit_post_mutation_trial_search_completeness_reconciliation.py` and `tests/validation/test_build_master_audit_post_mutation_trial_search_completeness_reconciliation.py`; updated `PROJECT_OUTLINE.md` with the command. Generated evidence: `reports/master_audit/master_audit_post_mutation_trial_search_completeness_reconciliation_20260710/master_audit_post_mutation_trial_search_completeness_reconciliation.{json,md}` with `PASS_MASTER_AUDIT_POST_MUTATION_TRIAL_SEARCH_COMPLETENESS_RECONCILIATION_REPORT_ONLY`, failure_count 0, 22 family-metadata rows, 10 search families, 2 multiple-testing families, 14 registry disposition notes, 4 experiment-ledger rows, `trial_ledger_search_path_complete=true`, and PBO/Deflated Sharpe/multiple-testing statuses `READY_FOR_SEPARATE_STATISTICAL_RECOMPUTE_TRIAL_LEDGER_INPUTS_COMPLETE`. This is input-completeness evidence only: `separate_statistical_recompute_required=true`, `statistical_recompute_executed=false`, `statistical_validity_ready=false`, `model_trust_ready=false`, `promotion_allowed=false`, paper/live false, and production false. Validation passed: focused pytest `8 passed`; approved regression group `25 passed`; live report command PASS with exactly the JSON/MD pair. No ledgers, registry, trial statuses, source reports, experiment ledger rows, data/model artifacts, Phase 8 outputs, gap maps, staging, commits, or pushes were mutated in this slice; no data/model commands, WFA/modeling, predictions, Phase 8 refresh, provider/network calls, promotion, artifact freeze, final holdout, paper/live, or cleanup ran.
- 2026-07-10 Master Audit canonical trial/search append-only mutation execution implemented and run with explicit approval token. Added `scripts/validation/apply_master_audit_canonical_trial_search_append_only_mutation_package.py` and `tests/validation/test_apply_master_audit_canonical_trial_search_append_only_mutation_package.py`; updated `PROJECT_OUTLINE.md` with the command. Canonical mutations executed: appended 14 `master_audit_disposition_notes` to `manifests/target_hypotheses/registry.json` and appended 22 family-metadata rows to `manifests/target_hypotheses/trial_statuses.jsonl`. Preserved 5 exclusion candidates as report-local only and appended 0 rows to `reports/experiments/ledger.jsonl`. Receipt evidence: `reports/master_audit/master_audit_canonical_trial_search_append_only_mutation_execution_20260710/master_audit_canonical_trial_search_append_only_mutation_execution.{json,md}` with `PASS_MASTER_AUDIT_CANONICAL_TRIAL_SEARCH_APPEND_ONLY_MUTATION_EXECUTION`, failure_count 0, `append_only_mutation_command_approved=true`, `canonical_mutation_executed=true`, registry notes 14, trial-status appends 22, trial-status rows 22 -> 44, experiment-ledger mutation false, statistical validity false, model trust false, and promotion false. Validation passed: focused pytest `7 passed`; approved regression group `17 passed`; live command PASS; receipt output pair exact. No source reports, experiment ledger rows, data/model artifacts, Phase 8 outputs, gap maps, staging, commits, or pushes were mutated; no data/model commands, WFA/modeling, predictions, Phase 8 refresh, provider/network calls, promotion, artifact freeze, final holdout, paper/live, or cleanup ran.
- 2026-07-10 Master Audit canonical trial/search append-only mutation package implemented and executed report-only. Added `scripts/validation/build_master_audit_canonical_trial_search_append_only_mutation_package.py` and `tests/validation/test_build_master_audit_canonical_trial_search_append_only_mutation_package.py`; updated `PROJECT_OUTLINE.md` with the new command. Generated evidence: `reports/master_audit/master_audit_canonical_trial_search_append_only_mutation_package_20260710/master_audit_canonical_trial_search_append_only_mutation_package.{json,md}` with `PASS_MASTER_AUDIT_CANONICAL_TRIAL_SEARCH_APPEND_ONLY_MUTATION_PACKAGE_REPORT_ONLY`, failure_count 0, 8/8 checks passed, decision `READY_FOR_SEPARATE_APPEND_ONLY_MUTATION_APPROVAL_REPORT_ONLY`, 14 registry note candidates, 22 trial-status family-metadata append candidates, 5 exclusion candidates, and 0 experiment-ledger append candidates. All canonical write/mutation approvals remain false: `append_only_mutation_command_approved=false`, `canonical_mutation_executed=false`, `registry_mutation_executed=false`, `trial_status_mutation_executed=false`, and `experiment_ledger_mutation_executed=false`; PBO, Deflated Sharpe, multiple-testing, statistical validity, model trust, promotion, paper/live, and production remain blocked. Validation passed: new focused pytest `8 passed`; approved regression group `37 passed`; live report command PASS with exactly the JSON/MD pair. No ledgers, registry, trial statuses, source reports, data/model artifacts, Phase 8 outputs, gap maps, staging, commits, or pushes were mutated; no data/model commands, WFA/modeling, predictions, Phase 8 refresh, provider/network calls, promotion, artifact freeze, final holdout, paper/live, or cleanup ran.
- 2026-07-10 Master Audit unresolved row-linkage/exclusion disposition implemented and executed report-only. Added `scripts/validation/build_master_audit_unresolved_row_linkage_exclusion_disposition.py` and `tests/validation/test_build_master_audit_unresolved_row_linkage_exclusion_disposition.py`; updated `PROJECT_OUTLINE.md` with the new command. Generated evidence: `reports/master_audit/master_audit_unresolved_row_linkage_exclusion_disposition_20260710/master_audit_unresolved_row_linkage_exclusion_disposition.{json,md}` with `PASS_MASTER_AUDIT_UNRESOLVED_ROW_LINKAGE_EXCLUSION_DISPOSITION_REPORT_ONLY`, failure_count 0, 7/7 checks passed, 5 unresolved rows, 4 legacy experiment-ledger rows, 1 current WFA/Phase8/statistical row, 0 linkable rows, and 5 report-local `EXCLUDE_FROM_CANONICAL_TRIAL_SEARCH_LEDGER` candidates. All canonical write/mutation approvals remain false; PBO, Deflated Sharpe, multiple-testing, statistical validity, model trust, promotion, paper/live, and production remain blocked. Validation passed: new focused pytest `9 passed`; approved regression group `29 passed`; live report command PASS with exactly the JSON/MD pair. No ledgers, registry, trial statuses, source reports, data/model artifacts, Phase 8 outputs, gap maps, staging, commits, or pushes were mutated; no data/model commands, WFA/modeling, predictions, Phase 8 refresh, provider/network calls, promotion, artifact freeze, final holdout, paper/live, or cleanup ran.
- 2026-07-10 Master Audit trial/search append-only disposition mutation decision implemented and executed report-only. Added `scripts/validation/build_master_audit_trial_search_append_only_disposition_mutation_decision.py` and `tests/validation/test_build_master_audit_trial_search_append_only_disposition_mutation_decision.py`; updated `PROJECT_OUTLINE.md` with the new command. Generated evidence: `reports/master_audit/master_audit_trial_search_append_only_disposition_mutation_decision_20260710/master_audit_trial_search_append_only_disposition_mutation_decision.{json,md}` with `PASS_MASTER_AUDIT_TRIAL_SEARCH_APPEND_ONLY_DISPOSITION_MUTATION_DECISION_REPORT_ONLY`, failure_count 0, 7/7 checks passed, decision `BLOCK_CANONICAL_APPEND_ONLY_MUTATION_PENDING_UNRESOLVED_LEGACY_AND_CURRENT_RUN_LINKAGE`, 27 candidate rows, 14 source disposition notes, 22 proposed family metadata rows, 5 unresolved rows, 14 draft registry disposition candidates, 22 draft trial-status append candidates, and 0 experiment/current WFA append candidates. All canonical write/mutation approvals remain false; PBO, Deflated Sharpe, multiple-testing, statistical validity, model trust, promotion, paper/live, and production remain blocked. Validation passed so far: new focused pytest `9 passed`; live report command PASS with exactly the JSON/MD pair. No ledgers, registry, trial statuses, source reports, data/model artifacts, Phase 8 outputs, gap maps, staging, commits, or pushes were mutated; no data/model commands, WFA/modeling, predictions, Phase 8 refresh, provider/network calls, promotion, artifact freeze, final holdout, paper/live, or cleanup ran.
- 2026-07-10 Master Audit unrecovered-source family-metadata remediation implemented and executed report-only. Added `scripts/validation/build_master_audit_unrecovered_source_family_metadata_remediation.py` and `tests/validation/test_build_master_audit_unrecovered_source_family_metadata_remediation.py`; updated `PROJECT_OUTLINE.md` with the new command. Generated evidence: `reports/master_audit/master_audit_unrecovered_source_family_metadata_remediation_20260710/master_audit_unrecovered_source_family_metadata_remediation.{json,md}` with `PASS_MASTER_AUDIT_UNRECOVERED_SOURCE_FAMILY_METADATA_REMEDIATION_REPORT_ONLY`, failure_count 0, 8/8 checks passed, 27 candidate trial/search ledger rows, 14 unrecovered source-report disposition notes, 21 registry JSON source-report refs, 7 present refs, 14 missing refs, report-local `search_family_id` proposals 22 and unresolved 5, and report-local `multiple_testing_family_id` proposals 22 and unresolved 5. All proposed irrecoverable-source dispositions are report-local only with canonical mutation disallowed. Preserved blockers: `append_only_backfill_allowed=false`, `write_schema_complete_rows_allowed=false`, `write_unresolved_marker_rows_allowed=false`, `ledger_mutation_allowed=false`, `ledger_mutation_executed=false`, `registry_mutation_executed=false`, `trial_status_mutation_executed=false`, `source_report_restore_executed=false`, `trial_ledger_search_path_complete=false`, `pbo_status=FAIL_MISSING_TRIAL_LOG`, `deflated_sharpe_status=FAIL_MISSING_TRIAL_LOG`, `multiple_testing_status=FAIL_MISSING_TRIAL_LOG`, `statistical_validity_ready=false`, `model_trust_ready=false`, `promotion_allowed=false`, `paper_live_ready=false`, and `production_ready=false`. Validation passed so far: new focused pytest `9 passed`; live report command PASS with exactly the JSON/MD pair in the new report root. No source reports were restored; no ledgers/registry/trial statuses, data/model artifacts, Phase 8 outputs, gap maps, staging, commits, or pushes were mutated; no data/model commands, WFA/modeling, predictions, Phase 8 refresh, provider/network calls, promotion, artifact freeze, final holdout, paper/live, or cleanup ran.
- 2026-07-10 Master Audit source-report recovery/disposition plan implemented and executed report-only. Added `scripts/validation/build_master_audit_source_report_recovery_disposition_plan.py` and `tests/validation/test_build_master_audit_source_report_recovery_disposition_plan.py`; updated `PROJECT_OUTLINE.md` with the new command. Generated evidence: `reports/master_audit/master_audit_source_report_recovery_disposition_plan_20260710/master_audit_source_report_recovery_disposition_plan.{json,md}` with `PASS_MASTER_AUDIT_SOURCE_REPORT_RECOVERY_DISPOSITION_PLAN_REPORT_ONLY`, failure_count 0, 7/7 checks passed, 14 missing registry JSON source reports, and disposition counts `UNRECOVERED_EXPLICIT_DISPOSITION_REQUIRED=14`. The plan found no recoverable exact local alternate or local git-history object for the 14 missing JSON source reports under the allowed read-only discovery scope. Preserved blockers: `append_only_backfill_allowed=false`, `ledger_mutation_allowed=false`, `source_report_restore_executed=false`, unresolved `search_family_id=27`, unresolved `multiple_testing_family_id=27`, `trial_ledger_search_path_complete=false`, `pbo_status=FAIL_MISSING_TRIAL_LOG`, `deflated_sharpe_status=FAIL_MISSING_TRIAL_LOG`, `multiple_testing_status=FAIL_MISSING_TRIAL_LOG`, `statistical_validity_ready=false`, `model_trust_ready=false`, `promotion_allowed=false`, `paper_live_ready=false`, and `production_ready=false`. Validation passed: new focused pytest `8 passed`; approved regression group `29 passed`; live report command PASS with exactly the JSON/MD pair. No source reports were restored; no ledgers/registry/trial statuses were backfilled or mutated; no data/model commands, WFA/modeling, predictions, Phase 8 refresh, generated report execution beyond the approved report-only command, provider/network calls, promotion, artifact freeze, final holdout, paper/live, cleanup, staging, commit, or push ran.
- 2026-07-10 Master Audit trial/search ledger backfill decision implemented and executed report-only. Added `scripts/validation/build_master_audit_trial_search_ledger_backfill_decision.py` and `tests/validation/test_build_master_audit_trial_search_ledger_backfill_decision.py`; updated `PROJECT_OUTLINE.md` with the new command. Generated evidence: `reports/master_audit/master_audit_trial_search_ledger_backfill_decision_20260710/master_audit_trial_search_ledger_backfill_decision.{json,md}` with `PASS_MASTER_AUDIT_TRIAL_SEARCH_LEDGER_BACKFILL_DECISION_REPORT_ONLY`, failure_count 0, decision `BLOCK_APPEND_ONLY_BACKFILL_PENDING_MISSING_SOURCE_REPORT_RECOVERY`, candidate rows 27, eligible rows 0, blocked rows 27, registry JSON source-report refs 21, present 7, missing 14, unresolved `search_family_id=27`, and unresolved `multiple_testing_family_id=27`. All write approvals are false: `append_only_backfill_allowed=false`, `write_schema_complete_rows_allowed=false`, `write_unresolved_marker_rows_allowed=false`, and `ledger_mutation_executed=false`. Preserved blockers: `trial_ledger_search_path_complete=false`, `pbo_status=FAIL_MISSING_TRIAL_LOG`, `deflated_sharpe_status=FAIL_MISSING_TRIAL_LOG`, `multiple_testing_status=FAIL_MISSING_TRIAL_LOG`, `pbo_applicability_ready=false`, `deflated_sharpe_applicability_ready=false`, `multiple_testing_applicability_ready=false`, `statistical_validity_ready=false`, `model_trust_ready=false`, `promotion_allowed=false`, `paper_live_ready=false`, and `production_ready=false`. Validation passed: new focused pytest `9 passed`; approved regression group `32 passed`; live report command PASS with exactly the JSON/MD pair. No ledgers/registry/trial statuses were backfilled or mutated; no data/model commands, WFA/modeling, predictions, Phase 8 refresh, statistical-validity generation, alpha matrix generation, gap-map generation, provider/network calls, promotion, artifact freeze, final holdout, paper/live, cleanup, staging, commit, or push ran.
- 2026-07-10 Master Audit Phase 2 active-scope causal/session reconciliation implemented and executed report-only. Added `scripts/validation/build_master_audit_phase2_active_scope_reconciliation.py` and `tests/validation/test_build_master_audit_phase2_active_scope_reconciliation.py`; updated `scripts/validation/build_master_audit_gap_remediation_evidence_map.py`, its tests, and `PROJECT_OUTLINE.md` so the gap map consumes the new active-scope Phase 2 evidence. Generated evidence: `reports/master_audit/master_audit_phase2_active_scope_reconciliation_20260710/master_audit_phase2_active_scope_reconciliation.{json,md}` with `PASS_MASTER_AUDIT_PHASE2_ACTIVE_SCOPE_RECONCILIATION_REPORT_ONLY`, failure_count 0, 11/11 checks passed, active scoped outputs 8, active-to-label hash matches 8, readiness `WARN`, severe blockers 0, scoped raw-without-causal 0, target timing `PASS_TARGET_TIMING_AUDIT`, target timing rows 2,837,374, `causal_session_active_scope_ready=true`, `phase2_full_master_audit_accepted=false`, `phase2_session_normalization_audit_accepted=false`, `model_trust_ready=false`, `promotion_allowed=false`, `paper_live_allowed=false`, and `production_ready=false`. Refreshed gap evidence: `reports/master_audit/master_audit_gap_remediation_after_phase2_20260710/master_audit_gap_remediation_evidence_map.{json,md}` with `source_acquisition_lineage=PASS`, `raw_row_parity_and_conversion=PASS`, `causal_session_active_scope=PASS`, overall `data_integrity=PASS`, statistical validity `FAIL`, operational resilience `FAIL`, `model_trust_ready=false`, `promotion_allowed=false`, `paper_live_ready=false`, and `production_ready=false`. Validation passed: focused pytest `52 passed`; live Phase 2 active-scope reconciliation PASS; live after-Phase-2 gap map PASS; Phase 1A registry validate-only returned 4526 rows / 0 failures; coordination docs PASS; `git diff --check` clean. No Phase 2 readiness/build, causal rebuild, parquet audit, label/feature rebuild, data/model command, WFA/modeling, predictions, Phase 8 refresh, provider/network call, promotion, artifact freeze, final holdout, paper/live, cleanup, staging, commit, or push ran.
- 2026-07-10 Master Audit Phase 1B broad raw row-parity/conversion reconciliation implemented and executed report-only. Added `scripts/validation/build_master_audit_phase1b_broad_reconciliation.py` and `tests/validation/test_build_master_audit_phase1b_broad_reconciliation.py`; updated `scripts/validation/build_master_audit_gap_remediation_evidence_map.py`, its tests, and `PROJECT_OUTLINE.md` so the gap map consumes the new broad Phase 1B evidence. Generated evidence: `reports/master_audit/master_audit_phase1b_broad_reconciliation_20260710/master_audit_phase1b_broad_reconciliation.{json,md}` with `PASS_MASTER_AUDIT_PHASE1B_BROAD_RECONCILIATION_REPORT_ONLY`, failure_count 0, 75/75 checks passed, 33 market reports, expected/raw market-years 527/527, needs conversion 0, raw-only 0, invalid manifests 0, source hash mismatches 0, definition join mismatches 0, raw schema failures 0, required-schema exception failures 0, accepted repair sources 6, legacy full alignment context `SUPERSEDED_BY_CORRECTED_EXPECTED_ONLY_BATCHES`, `raw_row_parity_and_conversion_ready=true`, `phase2_accepted=false`, `model_trust_ready=false`, and `promotion_allowed=false`. Refreshed gap evidence: `reports/master_audit/master_audit_gap_remediation_after_phase1b_20260710/master_audit_gap_remediation_evidence_map.{json,md}` with `source_acquisition_lineage=PASS`, `raw_row_parity_and_conversion=PASS`, `causal_session_active_scope=FAIL`, overall `data_integrity=FAIL`, statistical validity `FAIL`, operational resilience `FAIL`, `model_trust_ready=false`, and `promotion_allowed=false`. Validation passed so far in this slice: focused pytest `40 passed`; `python -m scripts.validation.phase1a_acquisition_registry --validate-only` returned 4526 rows / 0 failures; `python -m scripts.validation.check_coordination_docs` returned PASS. No Phase 1B conversion, raw/DBN alignment audit execution, Phase 2 readiness/build, data/model commands, WFA/modeling, predictions, Phase 8 refresh, provider/network calls, promotion, artifact freeze, final holdout, paper/live, cleanup, staging, commit, or push ran.
- 2026-07-10 Master Audit Phase 1A source-lineage reconciliation implemented and executed report-only. Added `scripts/validation/build_master_audit_phase1a_reconciliation.py` and `tests/validation/test_build_master_audit_phase1a_reconciliation.py`; updated `scripts/validation/build_master_audit_gap_remediation_evidence_map.py`, its tests, and `PROJECT_OUTLINE.md` so the gap map consumes direct Phase 1A evidence. Generated evidence: `reports/master_audit/master_audit_phase1a_reconciliation_20260710/master_audit_phase1a_reconciliation.{json,md}` with `PASS_MASTER_AUDIT_PHASE1A_RECONCILIATION_REPORT_ONLY`, failure_count 0, 10/10 checks passed, archive coverage `PASS`, expected archives 2108, missing archives/manifests/invalid manifests 0, registry rows 4526, registry validation failures 0, `phase1a_source_lineage_ready=true`, `original_delivery_reproducibility_ready=false`, `model_trust_ready=false`, and `promotion_allowed=false`. Refreshed gap evidence: `reports/master_audit/master_audit_gap_remediation_after_phase1a_20260710/master_audit_gap_remediation_evidence_map.{json,md}` with `source_acquisition_lineage=PASS` but `data_integrity=FAIL`, `raw_row_parity_and_conversion=FAIL`, `causal_session_active_scope=FAIL`, statistical validity `FAIL`, operational resilience `FAIL`, `model_trust_ready=false`, and `promotion_allowed=false`. Validation passed: focused pytest `26 passed`; `python -m scripts.validation.phase1a_acquisition_registry --validate-only` returned 4526 rows / 0 failures. No provider/network calls, DBN/raw/causal/label/feature rebuilds, WFA/modeling, predictions, Phase 8 refresh, promotion, artifact freeze, final holdout, paper/live, cleanup, staging, commit, or push ran.
- 2026-07-10 report-only Research Factory / Research Readiness evidence-map builder implemented but not executed against the live repo. Added `scripts/validation/build_master_audit_research_factory_readiness.py` and `tests/validation/test_build_master_audit_research_factory_readiness.py`. The builder consumes only coordination docs, existing Master Audit JSON/MD reports, Master Audit builder/test source filenames, and fresh `git status --short`; it writes only `master_audit_research_factory_readiness.{json,md}` when explicitly run. It preserves `Research Factory` / `Research Readiness` as blocked/not ready, fixes current-evidence readiness score at `20/100` (`Not Ready`), marks stale embedded run-status dirty/hash notes as historical instead of refreshing old hashes, and keeps all forbidden execution flags false. Focused validation passed: `python -m pytest -q -p no:cacheprovider tests\validation\test_build_master_audit_research_factory_readiness.py` (`10 passed`). No live builder report, existing Master Audit generator, phase audit, data/model command, WFA/modeling, prediction materialization, Phase 8 refresh, provider/network call, promotion, artifact freeze, final holdout, paper/live, production check, staging, commit, or push ran.
- 2026-07-09 Master Audit closeout and workspace commit-scope inventory completed report-only. Closeout evidence: `reports/master_audit/master_audit_closeout_20260709/master_audit_closeout.{json,md}` with `PASS_MASTER_AUDIT_CLOSEOUT_REPORT_ONLY`, failure_count 0, closeout `REPORT_ONLY_CLOSEOUT_BLOCKED_NOT_READY`, current line `closed_no_alpha_evidence`, current split `same_fold_rolling_retraining_research_only`, `full_master_audit_accepted=false`, `model_trust_ready=false`, `promotion_allowed=false`, `artifact_freeze_ready=false`, `final_holdout_ready=false`, `paper_live_ready=false`, and `production_ready=false`. Workspace inventory evidence: `reports/repo_hygiene/workspace_commit_scope_20260709/workspace_commit_scope_plan.{json,md}` with `PASS_WORKSPACE_COMMIT_SCOPE_PLAN_REPORT_ONLY`, failure_count 0, 95 dirty entries classified into 12 review scopes, and `auto_stage_allowed_count=0`. Added `scripts/validation/build_workspace_commit_scope_plan.py` and `tests/validation/test_build_workspace_commit_scope_plan.py`. Validation passed: targeted workspace pytest `7 passed`, live workspace report command PASS, prior closeout pytest `10 passed`, and no phase audits, data/model commands, WFA/modeling, predictions, Phase 8 refresh, provider/network calls, promotion, artifact freeze, final-holdout guard, paper/live, cleanup, staging, commit, push, DBN/raw/causal/label/feature/prediction parquet reads, model artifact reads, frozen artifact payload reads, hardened split candidate consumption, or hardened Phase 6/7 materialization path consumption ran.
- 2026-07-09 Phase 1B hostile DBN-to-raw-parquet row-parity audit support implemented report-only. `scripts/phase1C_validate/audit_raw_dbn_alignment.py` now has opt-in `--row-parity` evidence that decodes OHLCV DBNs, hashes OHLCV rows including integer `ts_event` nanoseconds, checks exact source/output row counts, missing/output-only rows, duplicate collapse without a dedup ledger, source row order changes, raw timestamp timezone, and failed-row samples. It also reports raw column classifications and opt-in `--require-source-only-raw` failures for derived/enrichment columns. Focused tests added in `tests/validation/test_audit_raw_dbn_alignment.py`; validation passed: `python -m pytest -q -p no:cacheprovider tests\validation\test_audit_raw_dbn_alignment.py` (`37 passed`). No provider calls, DBN/raw/causal/label/feature/model data mutation, live data row-parity report generation, WFA/modeling, staging, commit, push, cleanup, or paper/live work ran.
- 2026-07-09 Phase 3 label semantics v2 is implemented in code/tests and active `data/labeled` was replaced for Tier 1 core only: `6E/CL/ES/ZN` years `2023/2024`. Contract: 30m primary, independent 60m robustness, diagnostic-only 15m, Apex-aware cost/path/risk/close/fillability gates, 61-bar purge, semantics id `phase3_labels_v2_next_1m_open_30m_primary_60m_confirm_apex_aware`. Updated Phase 3/4 label contracts, leakage guard, model registry configs/tests, and `PROJECT_OUTLINE.md`. Broader 518-pair active labels were not rebuilt or replaced.
- 2026-07-09 bounded Phase 3/4/5 v2 active core path completed for exactly `6E/CL/ES/ZN` years `2023/2024`. Phase 3 active replacement evidence: `reports/labels/phase3_v2_apex_30m60m_20260709_active_replacement_decision/`, status `PASS_ACTIVE_LABEL_REPLACEMENT_V2_CORE_NO_DOWNSTREAM`, backup root `data/labeled_active_backup/phase3_v2_apex_30m60m_20260709_tier1_core_pre_replace/`, active hashes match staged 8/8. Phase 4 active placement evidence: `reports/features_baseline/phase4_v2_apex_30m60m_20260709_tier1_core_active_placement/`, status `PASS_ACTIVE_FEATURE_PLACEMENT_V2_CORE_NO_DOWNSTREAM`, backup root `data/feature_matrices_active_backup/phase4_v2_apex_30m60m_20260709_tier1_core_pre_replace/`, 8 files plus 4 sidecars, 2,837,374 rows, 2,191,100 training-valid rows, 114 features, active hashes match staged, backups match pre-active. Report-only Phase 5 preflight `reports/wfa_preflight/phase5_v2_apex_30m60m_20260709_tier1_core/phase5_preflight_decision.{json,md}` passed `PASS_PHASE5_V2_WFA_PREFLIGHT_READY_NO_SPLIT_BUILD`. Bounded Phase 5 split generation then wrote only `reports/wfa/phase5_v2_apex_30m60m_20260709_tier1_core/split_plan.{json,csv}`; split manifest `PASS`, 48 folds, 12 per market, profile `tier_1`, resolved `tier_1_research`, purge/embargo 61/61, feature-manifest gate PASS, data-audit universe exact usable evidence. Acceptance review `split_plan_acceptance_report.{json,md}` passed `PASS_PHASE5_SPLIT_PLAN_ACCEPTED_REPORT_ONLY`, failure_count 0, scoped prediction_file_count 0. Validation: focused Phase 5 tests `26 passed`; coordination docs `PASS`; `git diff --check` passed with CRLF warnings only; generated roots are ignored. No Phase 6/WFA modeling, Phase 8, provider, promotion, artifact freeze, final holdout, paper/live, cleanup, git staging, commit, push, rescue tuning, label/feature rebuild, or prop-account reports ran.
- 2026-07-09 bounded Phase 6 v2 WFA runner preflight and report-only WFA execution completed for the accepted active v2 Tier 1 core scope. Preflight evidence root `reports/wfa/phase6_v2_apex_30m60m_20260709_tier1_core_runner_preflight/` passed `PASS_PHASE6_WFA_RUNNER_PREFLIGHT_READY_REPORT_ONLY` with 48 folds, 114 features, 8 models, zero scoped prediction files, and no runner execution. Report-only WFA command used `--report-only` without `--predictions-root` or `--write-predictions`; the shell wrapper returned timeout code 124 after printing `PASS WFA baseline`, but generated artifacts validated cleanly under `reports/wfa/phase6_v2_apex_30m60m_20260709_tier1_core_report_only/`: `_wfa_report.json` and `_predictions_manifest.json` only, failure_count 0, warning_count 0, fold_count 48, unfiltered_selectable_fold_count 48, prediction_count 8,517,568 in memory, duplicate_prediction_count 0, prediction_writes_enabled false, prediction_artifact_written false, prediction_artifact_write_skipped true, prediction_path/output_root/predictions_root null, prediction_markets `6E/CL/ES/ZN`, split-plan years `2023/2024`, OOS prediction_years `[2024]` because all test windows are 2024, and artifact_evidence_ready true. Focused validation passed: `python -m pytest -q -p no:cacheprovider tests/phase7_wfa/test_run_wfa.py tests/validation/test_review_phase6_wfa_runner_preflight.py` (`69 passed`); report artifact validation PASS; expected generated file scope PASS; coordination docs PASS; `git diff --check` exited 0 with CRLF warnings only; scoped generated roots are ignored. Restored legacy non-prefixed label-like names such as `trend_danger_up_30m` to `scripts/validation/feature_leakage_guard.py` so frozen WFA feature sets fail closed. No prediction parquet, Phase 8, provider, promotion, artifact freeze, final holdout, paper/live, cleanup, git staging, commit, push, rescue tuning, label/feature rebuild, Phase 5 rerun, or prop-account reports ran.
- 2026-07-09 WFA split-contamination guard, split-hardening decision, hardened split-builder implementation, bounded hardened Phase 5 split candidate generation, report-only Phase 6/7 hardened-split compatibility decision, and hardened Phase 6 runner preflight completed for active v2 Tier 1 core. Contamination evidence remains `reports/wfa/phase5_v2_apex_30m60m_20260709_tier1_core_contamination_audit/wfa_split_contamination_audit.{json,md}` with classification `same_fold_rolling_retraining_research_only`, independent holdout disallowed, and rolling-retraining research evidence allowed. Hardening decision evidence remains `reports/wfa/phase5_v2_apex_30m60m_20260709_tier1_core_split_hardening_decision/split_hardening_decision.{json,md}`, status `PASS_SPLIT_HARDENING_DESIGN_FEASIBLE_NO_SPLIT_BUILD`. Builder/validator/compatibility files: `scripts/phase5_wfa/build_hardened_wfa_splits.py`, `scripts/validation/validate_hardened_wfa_split_plan.py`, `scripts/validation/build_phase6_hardened_split_compatibility_decision.py`, `tests/phase5_wfa/test_build_hardened_wfa_splits.py`, `tests/validation/test_validate_hardened_wfa_split_plan.py`, and `tests/validation/test_build_phase6_hardened_split_compatibility_decision.py`. Generated ignored hardened candidate root: `reports/wfa/phase5_v2_apex_30m60m_20260709_tier1_core_hardened_split_candidate/` with exactly `split_plan.{json,csv}` and `hardened_split_acceptance_report.{json,md}`. Split status `PASS_HARDENED_PHASE5_SPLIT_PLAN_BUILT_NO_MODELING`, acceptance status `PASS_HARDENED_PHASE5_SPLIT_PLAN_ACCEPTED_NO_MODELING`, failure_count 0, fold_count 4, one fold each for `6E/CL/ES/ZN`, years `[2023,2024]`, `independent_test_claim_allowed=true`, `modeling_allowed=false`, and `prediction_materialization_allowed=false`. Compatibility evidence root `reports/wfa/phase6_v2_apex_30m60m_20260709_tier1_core_hardened_split_compatibility/` has `PASS_PHASE6_HARDENED_SPLIT_PREFLIGHT_COMPATIBLE_NO_WFA_EXECUTION`, failure_count 0, six PASS checks, `phase6_preflight_compatible=true`, `phase7_decision=DEFER_PHASE7_NO_HARDENED_PREDICTIONS`, and exposed candidate preflight command only. Hardened runner preflight evidence root `reports/wfa/phase6_v2_apex_30m60m_20260709_tier1_core_hardened_runner_preflight/` has exactly `wfa_runner_preflight_report.{json,md}` and `tier1_core_active_feature_set.json`; status `PASS_PHASE6_WFA_RUNNER_PREFLIGHT_READY_REPORT_ONLY`, failure_count 0, 114 features, 4 folds, 8 models, `commands_executed=0`, `model_training_performed=false`, `prediction_generation_performed=false`, scoped `prediction_file_count=0`, and no `data/predictions/phase6_v2_apex_30m60m_20260709_tier1_core_hardened` root. Validation passed: focused hardened/preflight pytest `15 passed`, live compatibility and hardened runner preflight commands PASS, coordination docs PASS, and `git diff --check` exited 0 with existing CRLF warnings only. No WFA execution, prediction generation/materialization, Phase 8, provider/download, promotion, artifact freeze, final holdout, paper/live, cleanup, staging, commit, push, rescue tuning, label/feature rebuild, active split replacement, or prop-account reports ran.
- 2026-07-09 hardened Phase 6 prediction-materialization approval decision implemented and executed report-only. Added `scripts/validation/build_phase6_hardened_prediction_materialization_decision.py` and `tests/validation/test_build_phase6_hardened_prediction_materialization_decision.py`; `PROJECT_OUTLINE.md` now lists the report-only command. Generated ignored root `reports/wfa/phase6_v2_apex_30m60m_20260709_tier1_core_hardened_prediction_materialization_decision/` contains exactly `prediction_materialization_decision.{json,md}`. Decision status is `BLOCKED_PHASE6_HARDENED_PREDICTION_MATERIALIZATION_RUNNER_GUARD_NO_EXECUTION`, failure_count 0, 14 PASS checks, `candidate_command_approved_for_execution=false`, `prediction_materialization_executed=false`, `future_runner_patch_required=true`, and runner hardened write guard active. The exact future `python -m scripts.phase6_wfa.run_wfa ... --write-predictions` command is recorded as not approved. Confirmed no `reports/wfa/phase6_v2_apex_30m60m_20260709_tier1_core_hardened` root and no `data/predictions/phase6_v2_apex_30m60m_20260709_tier1_core_hardened` root. Validation passed: focused pytest `80 passed`; live report-only command PASS; strict output check found only the two decision files. No WFA execution, prediction parquet materialization, Phase 8, provider/download, promotion, artifact freeze, final holdout, paper/live, cleanup, staging, commit, push, rescue tuning, label/feature rebuild, active split replacement, or prop-account reports ran.
- Post-KE `all_raw` raw/DBN alignment refresh exists at `reports/data_audit/current_state/post_replacement_all_raw_dbn_alignment_20260705.{json,md}` and passed, but it predates the SR1/SR3 2020 active raw replacement.
- The refreshed PASS still relies on two accepted repair-source exceptions for SR1/SR3 2020. Follow-up no-mutation reconciliation is `reports/data_audit/current_state/sr1_sr3_2020_repair_source_reconciliation_20260705.{json,md}`: current DBN manifests for `ohlcv_1m_parent`, `definition`, `statistics`, and `status` are valid, and raw OHLCV hashes match current parent OHLCV files, but raw status/statistics lineage hashes match archived `dbn_sr_parent_candidate` sidecars rather than current active sidecars. Provider re-download is not indicated by that report.
- Approved no-download staged local raw regeneration for SR1/SR3 2020 was attempted and blocked by source-audit policy before writing any staged parquet. Report: `reports/data_audit/sr1_sr3_2020_current_sidecar_raw_rebuild_20260705/sr_front_contract_candidate_manifest.json`. Failure reason: current `data/dbn/status/{SR1,SR3}/2020` and `data/dbn/statistics/{SR1,SR3}/2020` manifests are `stype_in=continuous`; explicit monotonic SR roll repair requires parent or explicit-contract status/statistics DBNs. `data/raw_parent_consolidation_staging/sr1_sr3_2020_current_sidecars_20260705` was not created.
- Bounded provider acquisition for SR1/SR3 2020 parent sidecars completed into staging only: `data/dbn/sr1_sr3_2020_parent_sidecar_staging_20260705/{status,statistics}/{SR1,SR3}/2020/2020-01-01_2021-01-01.dbn.zst`. Acquisition reports are under `reports/data_audit/sr1_sr3_2020_parent_sidecar_acquisition_20260705/`. Both status and statistics zero-cost gates passed with 2/2 selected tasks, provider jobs completed with cost `$0.0000`, and file-level SHA-256 checks matched all four staged sidecar manifests. No active `data/dbn/status`, `data/dbn/statistics`, or `data/raw` files were intentionally overwritten.
- Bounded local staged raw regeneration for SR1/SR3 2020 using the staged parent sidecars completed with `PASS front-contract candidate build: outputs=2 failures=0`. Staged outputs are `data/raw_parent_consolidation_staging/sr1_sr3_2020_parent_sidecars_20260705/SR1/2020.parquet` and `.../SR3/2020.parquet`; reports are under `reports/data_audit/sr1_sr3_2020_parent_sidecar_raw_rebuild_20260705/`. Manifest and raw-alignment status are `PASS`, source-hash mismatches are 0, staged row counts are SR1 8,566 and SR3 3,461, duplicate `ts_event` rows are 0, and output hashes match the manifest.
- Bounded active raw replacement for SR1/SR3 2020 completed for only `data/raw/SR1/2020.parquet` and `data/raw/SR3/2020.parquet` from the validated staged outputs. Replacement report: `reports/data_audit/sr1_sr3_2020_parent_sidecar_raw_replacement_20260705.json`, status `PASS`, failures 0, active raw hashes equal staged/manifest hashes, row counts are SR1 8,566 and SR3 3,461, duplicate `ts_event` rows are 0, and active raw lineage now points to `data/dbn/ohlcv_1m_parent` plus the staged parent `status`/`statistics` sidecars. No provider calls, DBN promotion, sidecar canonicalization, cleanup/archive, or downstream causal/label/feature/WFA/prediction work ran.
- Report-only current-state `all_raw` alignment after the SR1/SR3 replacement now exists at `reports/data_audit/current_state/post_sr_replacement_all_raw_dbn_alignment_20260705.{json,md}` with repair manifest `reports/data_audit/current_state/post_sr_replacement_raw_alignment_repair_manifest_20260705.json`. Direct artifact verification confirms status `PASS`, expected/raw 530/530, missing raw 0, raw-only 0, invalid manifests 0, source-hash mismatches 0, definition-join mismatches 0, repair manifest status `PASS`, and accepted repair sources 2 for SR1/SR3 2020 pointing to `data/dbn/ohlcv_1m_parent`. The shell wrapper timed out after printing the PASS summary, so acceptance is based on direct JSON/Markdown artifact verification. The generic all_raw checker validates OHLCV source hashes and definition joins; the SR1/SR3 status/statistics sidecar lineage remains proven by `reports/data_audit/sr1_sr3_2020_parent_sidecar_raw_replacement_20260705.json`.
- The SR1/SR3 2020 historical repair-source caveat is accepted as resolved for data-consolidation purposes. Refreshed no-mutation current-state ledger: `reports/data_audit/current_state/data_current_state_20260705T153606Z.{json,md}`. Ledger status is `PASS`, records the acceptance decision, cites the post-SR all_raw alignment and SR replacement report, and explicitly does not approve cleanup, archive moves, DBN sidecar promotion, data replacement, causal/label/feature rebuilds, WFA, predictions, promotion, paper, or live trading.
- Bounded readiness-only active-root review now exists at `reports/data_audit/current_state/active_root_raw_to_causal_readiness_20260705.{json,md}` from `scripts.validation.review_active_root_causal_readiness`. Status is `WARN`: active raw is 530 pairs, active causal is 518 pairs, 518 active causal files match historical output hash/row evidence, causal-without-raw is 0, causal-without-evidence is 0, but 6 active causal files were built from stale raw hashes (`KE:2019`, `KE:2021`, `KE:2023`, `KE:2024`, `SR1:2020`, `SR3:2020`), 12 active raw pairs have no active causal output (`6E:2025`, `6E:2026`, `6M:2012`, `CL:2025`, `CL:2026`, `ES:2025`, `ES:2026`, `TN:2010`, `TN:2011`, `TN:2012`, `ZN:2025`, `ZN:2026`), and local-trade/OHLCV gap gate status remains `NOT_RUN` for all 518 evidence rows. No provider calls, data mutation, cleanup/archive, DBN sidecar canonicalization, label/feature/WFA/prediction/modeling, commit, push, paper, or live action ran.
- Bounded report-only causal rebuild/disposition plan now exists at `reports/data_audit/current_state/active_root_causal_rebuild_disposition_plan_20260705.{json,md}` from `scripts.validation.plan_active_root_causal_rebuild_disposition`. Status is `PASS_PLAN_READY_NO_EXECUTION`: scope rows 18, `rebuild=6`, `exclude=0`, `defer=12`. The six rebuild rows are `KE:2019`, `KE:2021`, `KE:2023`, `KE:2024`, `SR1:2020`, and `SR3:2020`; all twelve raw-without-causal rows remain deferred pending separate policy disposition. No provider calls, data mutation, causal parquet writes, cleanup/archive, DBN sidecar canonicalization, label/feature/WFA/prediction/modeling, commit, push, paper, or live action ran.
- Bounded causal-only rebuild completed for exactly six previously stale raw-input causal rows: `KE:2019`, `KE:2021`, `KE:2023`, `KE:2024`, `SR1:2020`, and `SR3:2020`. The build first wrote validated staging outputs under `data/causally_gated_normalized_rebuild_staging/active_root_six_stale_raw_rebuild_20260705/`, then copied only those six parquet files into `data/causally_gated_normalized/{market}/{year}.parquet`. Evidence: include list `reports/data_audit/current_state/active_root_six_causal_rebuild_include_20260705.json`, build reports under `reports/data_audit/causal_base_rebuild/active_root_six_stale_raw_rebuild_20260705/`, and post-copy validation `reports/data_audit/current_state/active_root_six_causal_rebuild_validation_20260705.{json,md}` with status `PASS`, six active files matching staged hashes, six staged parquet files only, manifest status `PASS`, readiness status `PASS`, and zero row validation failures. No provider calls, `data/raw` or `data/dbn` mutation, cleanup/archive, DBN sidecar canonicalization, label/feature/WFA/prediction/modeling, commit, push, paper, or live action ran.
- Bounded report-only current-state refresh after the six-row causal rebuild completed. Refreshed readiness is `reports/data_audit/current_state/active_root_raw_to_causal_readiness_post_six_causal_rebuild_20260705.{json,md}` with status `WARN`: active raw 530, active causal 518, historical evidence 518, stale raw-input hash rows 0, active causal hash mismatches 0, active causal row mismatches 0, causal-without-evidence 0, raw-without-causal 12, severe blockers 0, medium blockers 2. Refreshed ledger is `reports/data_audit/current_state/data_current_state_20260705T161444Z.{json,md}` with status `PASS` and readiness status `WARN`. No provider calls, `data/raw` or `data/dbn` mutation, causal writes, cleanup/archive, sidecar canonicalization, label/feature/WFA/prediction/modeling, commit, push, paper, or live action ran.
- Bounded report-only policy disposition for the 12 post-rebuild raw-without-causal rows completed. Report: `reports/data_audit/current_state/active_root_raw_without_causal_policy_disposition_20260705.{json,md}` with status `PASS_POLICY_DISPOSITION_READY_NO_EXECUTION`, scope rows 12, `rebuild=0`, `exclude=0`, `defer=12`. It recommends deferring `6E:2025`, `6E:2026`, `CL:2025`, `CL:2026`, `ES:2025`, `ES:2026`, `ZN:2025`, and `ZN:2026` as holdout/forward raw rows pending separate holdout/forward causal-scope policy; `6M:2012` as a roll-maturity blocker candidate pending separate roll-maturity policy; and `TN:2010`, `TN:2011`, `TN:2012` as early-TN raw extras pending separate instrument-scope policy. No provider calls, `data/raw` or `data/dbn` mutation, causal parquet writes, cleanup/archive, sidecar canonicalization, label/feature/WFA/prediction/modeling, commit, push, paper, or live action ran.
- The 12-row report-only `defer` disposition is accepted for current planning. Refreshed current-state ledger is `reports/data_audit/current_state/data_current_state_20260705T162319Z.{json,md}` with status `PASS`, two accepted decisions, `defer=12`, `rebuild=0`, `exclude=0`, readiness status `WARN`, stale raw-input hash rows 0, and raw-without-causal rows 12. This acceptance is not a rebuild or exclusion approval, and it does not approve provider calls, data mutation, cleanup/archive, sidecar canonicalization, labels/features/WFA/predictions/modeling, commit, push, paper, or live action.
- Bounded report-only cleanup/archive no-touch disposition completed for staging/archive/candidate roots. Report: `reports/data_audit/current_state/cleanup_archive_no_touch_disposition_20260705.{json,md}` with status `PASS_NO_TOUCH_PLAN_READY_NO_EXECUTION`, 12 scoped roots, 6 `protect_no_touch`, 2 `review_required_no_touch`, 4 `absent_no_action`, and 0 approved file moves, deletes, or data mutations. Protected roots include the SR parent sidecar staging root, raw parent consolidation staging, causal rebuild staging, DBN promotion/archive parent-consolidation roots, and `data/archive`; `archive` and empty `data/dbn_sr_parent_candidate` require separate review before any cleanup/archive execution. No provider calls, data mutation, cleanup/archive execution, sidecar canonicalization, labels/features/WFA/predictions/modeling, commit, push, paper, or live action ran.
- Bounded report-only downstream causal-to-label readiness review completed after accepting the cleanup/archive no-touch planning evidence. Report: `reports/data_audit/current_state/active_root_causal_to_label_readiness_20260705.{json,md}` with status `BLOCKED_NOT_READY_NO_EXECUTION`: 518 active causal pairs, 405 active-pattern label pairs, 404 matched pairs, 114 causal-without-active-label pairs, 1 active-label-without-active-causal pair, 54 matched row-count mismatches, 8 nested non-active label files, and 0 current PASS full active-root causal manifests. No provider calls, data mutation, cleanup/archive execution, sidecar canonicalization, label writes, feature/WFA/prediction/modeling, commit, push, paper, or live action ran.
- Bounded report-only label rebuild/disposition plan completed. Report: `reports/data_audit/current_state/active_root_label_rebuild_disposition_plan_20260705.{json,md}` with status `PASS_PLAN_READY_NO_EXECUTION` and execution readiness `BLOCKED_PENDING_ACTIVE_CAUSAL_MANIFEST_GATE`: 1 report-only active causal manifest gate precondition, 168 label rebuild candidates after manifest-gate acceptance, 1 active-label-without-active-causal row excluded from active label scope with no file touch, 0 defers, 350 row-count-matching labels left out of scope with lineage still unverified, and 8 nested non-active label files out of scope. No provider calls, data mutation, cleanup/archive execution, sidecar canonicalization, label writes, feature/WFA/prediction/modeling, commit, push, paper, or live action ran.
- Bounded report-only active causal manifest gate refresh completed for exactly 518 active causal pairs under `data/causally_gated_normalized`. Evidence root: `reports/data_audit/current_state/active_root_causal_manifest_gate_20260705/`; manifest: `causal_base_manifest.json`; report: `active_causal_manifest_gate_report.{json,md}`. Status is `PASS_MANIFEST_GATE_READY_NO_EXECUTION`: manifest status `PASS`, Phase 3 manifest-gate check `PASS`, gate failures 0, active causal pairs 518, total causal rows 153,213,286. No provider calls, data mutation, cleanup/archive execution, sidecar canonicalization, label writes, feature/WFA/prediction/modeling, commit, push, paper, or live action ran.
- Bounded staged label rebuild completed for exactly the 168 `rebuild_label_after_manifest_gate` rows from `reports/data_audit/current_state/active_root_label_rebuild_disposition_plan_20260705.{json,md}`. Staged outputs are under `data/labeled_rebuild_staging/active_root_label_rebuild_20260705/`; validation evidence is under `reports/data_audit/current_state/active_root_label_rebuild_staging_20260705/`. Validation status is `PASS_STAGED_LABEL_REBUILD_READY_NO_ACTIVE_REPLACEMENT`: 168 staged parquet files, label report `PASS`, label manifest `PASS`, 168 pass / 0 warn / 0 fail label results, input/output rows 43,057,061, active label hash changes 0, validation failures 0, and no staged `6M:2012.parquet`. No active `data/labeled` overwrite, provider calls, data/raw/data/dbn/data/causally_gated_normalized mutation, cleanup/archive execution, sidecar canonicalization, feature/WFA/prediction/modeling, commit, push, paper, or live action ran.
- Bounded active label replacement completed for exactly the 168 validated staged files under `data/labeled_rebuild_staging/active_root_label_rebuild_20260705/`. Replacement evidence is `reports/data_audit/current_state/active_root_label_replacement_20260705/active_label_replacement_report.{json,md}` with status `PASS_ACTIVE_LABEL_REPLACEMENT_NO_DOWNSTREAM`: copied 168 files, destination hashes match staged sources for all 168, 54 destinations existed before, 114 were missing before, destination rows total 43,057,061, `data/labeled/6M/2012.parquet` was unchanged, and nested `data/labeled/tier1_rebuild_v1` was unchanged. Post-replacement alignment is `reports/data_audit/current_state/post_label_replacement_causal_to_label_alignment_20260705.{json,md}` with status `WARN_LABEL_ONLY_EXTRA_NO_EXECUTION`: active causal pairs 518, active label pairs 519, matched pairs 518, causal-without-label 0, row-count mismatches 0, active-label-without-causal 1, and nested non-active label files 8. No provider calls, data/raw/data/dbn/data/causally_gated_normalized mutation, cleanup/archive execution, sidecar canonicalization, feature/WFA/prediction/modeling, commit, push, paper, or live action ran.
- Bounded report-only active label manifest/readiness gate completed for exactly the 518 matched active causal-to-label pairs. Evidence root: `reports/data_audit/current_state/active_root_label_manifest_gate_20260705/`; manifest: `label_base_manifest.json`; report: `active_label_manifest_gate_report.{json,md}`. Status is `WARN_ACTIVE_LABEL_MANIFEST_GATE_READY_NO_DOWNSTREAM`: label manifest status `PASS`, 518 scoped pairs, 518 pass, 0 fail, 153,213,286 scoped label rows, 0 row-count mismatches, 0 causal hash mismatches, 0 missing required label-column pairs, 6 label schema fingerprints, and one known label-only extra (`6M:2012`) outside scope. No provider calls, data mutation, cleanup/archive execution, sidecar canonicalization, feature/WFA/prediction/modeling, commit, push, paper, or live action ran.
- Accepted the active label manifest/readiness gate as report-only evidence and ran a bounded report-only label-to-feature readiness/disposition review. Report root: `reports/data_audit/current_state/active_root_label_to_feature_readiness_20260705/`; report: `active_root_label_to_feature_readiness_disposition.{json,md}`. Status is `WARN_LABEL_TO_FEATURE_READY_FOR_STAGED_BUILD_NO_EXECUTION`: 518 scoped label-manifest pairs, 518 ready for staged feature build after separate bounded approval, 0 defer, 0 exclude, 153,213,286 scoped label rows, 33 markets, 17 years, 122 baseline Phase 4 features, 0 forbidden feature names, 0 target-like feature names, 0 label-manifest hash/row mismatches, and 0 missing required Phase 4 label-column pairs. The only warning is the inherited 6 label schema fingerprints, so any feature build should be staged and audited before active replacement. No feature writes, WFA, predictions, modeling, cleanup/archive, provider calls, commit, push, paper, or live work ran.
- Bounded staged Phase 4 feature build attempt for the 518 ready label-manifest rows is blocked and incomplete. Evidence root: `reports/features_baseline/active_root_baseline_features_20260705/`; blocker report: `phase4_staged_feature_build_blocker.{json,md}` with status `BLOCKED_PHASE4_STAGED_FEATURE_BUILD_INCOMPLETE`. The planned direct builder command first failed its label-manifest gate because the active label manifest is an audit-gate refresh manifest with warnings, not a plain `stage=labels` / `profile=all_labeled` manifest. A report-only Phase 4 input manifest adapter was created from the accepted 518-row active label manifest, excluding `6M:2012`, and preflight passed. The non-6M builder chunk then wrote reports and partial staged outputs but returned `FAIL`: 502 selected files, 93 pass, 73 warn, 336 fail. The primary failure reason is missing required Phase 3 label columns `target_trend_adverse_long_30m`, `target_trend_favorable_long_30m`, `target_trend_adverse_short_30m`, and `target_trend_favorable_short_30m` in 336 rows. The staged root `data/feature_matrices_rebuild_staging/active_root_baseline_features_20260705` contains 166 parquet files / 170 total files / about 9.5 GB, but it is partial and not valid for downstream use. The 6M chunk was not run. No active feature replacement, WFA, predictions, modeling, provider calls, cleanup/delete/archive execution, commit, push, paper, or live action ran.
- Bounded report-only Phase 3/4 archive reset plan completed. Report root: `reports/data_audit/current_state/phase3_phase4_archive_reset_plan_20260705/`; report: `phase3_phase4_archive_reset_plan.{json,md}`. Status is `PASS_PHASE3_PHASE4_ARCHIVE_RESET_PLAN_READY_NO_EXECUTION`: scoped roots are `data/labeled`, `data/feature_matrices`, and `data/feature_matrices_rebuild_staging`; 701 files / 693 parquet files / 17,735,912,846 bytes were counted and hashed. Plan classifies `data/labeled` and `data/feature_matrices` as `archive_active_root_contents_keep_empty_root`, preserving the top-level active roots as empty rebuild roots after a future approved move, and classifies `data/feature_matrices_rebuild_staging` as `archive_full_staging_root_remove_from_data`. Planned archive root is `archive/phase3_phase4_archive_reset_20260705`. No file moves/deletes, provider calls, feature rebuild retries, active feature replacement, cleanup/archive execution, WFA, predictions, modeling, commit, push, paper, or live action ran.
- Bounded path-level Phase 3/4 archive reset execution completed from the approved plan. Evidence: `reports/data_audit/current_state/phase3_phase4_archive_reset_plan_20260705/phase3_phase4_archive_reset_execution_precheck.json` and `phase3_phase4_archive_reset_execution_post.{json,md}` with status `PASS_PHASE3_PHASE4_ARCHIVE_RESET_EXECUTED`: precheck matched 701 files / 693 parquet files / 17,735,912,846 bytes, all archived hashes match the precheck, `data/labeled` and `data/feature_matrices` remain present as empty rebuild roots, `data/feature_matrices_rebuild_staging` is absent, protected Phase 1/2 roots still exist, and top-level `data` contains only `causally_gated_normalized`, `dbn`, `feature_matrices`, `labeled`, `predictions`, and `raw`. No deletion, provider calls, rebuilds, active replacement, WFA, predictions, modeling, commit, push, paper, or live action ran.
- Bounded report-only nested Tier1 label disposition completed for `data/labeled/tier1_rebuild_v1`. Report root: `reports/data_audit/current_state/nested_tier1_label_disposition_20260705/`; report: `nested_tier1_label_disposition.{json,md}`. Status is `WARN_NESTED_TIER1_LABEL_DISPOSITION_READY_NO_EXECUTION`: the nested root contains exactly 8 parquet files for `6E`, `CL`, `ES`, and `ZN` in 2023-2024; all 8 active direct `data/labeled/{market}/{year}.parquet` files exist and match the active label manifest hashes; all 8 nested hashes differ from active and manifest hashes; row counts match active, but nested files have 151 columns while active manifest files have 90 columns. Do not overwrite active labels from the nested root. Recommended future cleanup is a separately approved move of `data/labeled/tier1_rebuild_v1` outside `data` after rerunning the hash precheck. No data move/delete/overwrite, provider calls, cleanup/archive execution, sidecar canonicalization, feature/WFA/prediction/modeling, commit, push, paper, or live action ran.
- Bounded path-level nested Tier1 label move completed after rerunning the approved hash precheck. Evidence: `reports/data_audit/current_state/nested_tier1_label_disposition_20260705/nested_tier1_label_move_precheck.json` and `nested_tier1_label_move_post_execution.{json,md}`. Post-execution status is `PASS_NESTED_TIER1_LABEL_MOVE_EXECUTED`: `data/labeled/tier1_rebuild_v1` is absent, `archive/nested_tier1_label_disposition_20260705/data_labeled_tier1_rebuild_v1` exists with 8 parquet files, moved hashes match precheck nested hashes 8/8, active label hashes are unchanged from precheck 8/8, active label hashes still match the current label manifest 8/8, and moved hashes match the label manifest 0/8. No active label overwrite, provider calls, sidecar canonicalization, feature/WFA/prediction/modeling, commit, push, paper, or live action ran.
- Bounded report-only final data-root cleanup execution plan completed for exactly `data/labeled_rebuild_staging`, `data/causally_gated_normalized_rebuild_staging`, `data/raw_parent_consolidation_staging`, `data/dbn_sr_parent_candidate`, and `data/archive`. Report root: `reports/data_audit/current_state/final_data_root_cleanup_execution_plan_20260705/`; report: `final_data_root_cleanup_execution_plan.{json,md}`. Status is `PASS_FINAL_DATA_ROOT_CLEANUP_EXECUTION_PLAN_READY_NO_EXECUTION`: 3 `delete-after-hash-match` roots, 1 `remove-empty` root, 1 `move-outside-data-archive` root, 0 `keep-protected`, and post-execution `data` roots would match the six-root allow-list (`data/labeled`, `data/dbn`, `data/predictions`, `data/feature_matrices`, `data/causally_gated_normalized`, `data/raw`). The three staging roots all hash-match their active destinations, `data/dbn_sr_parent_candidate` is empty, and `data/archive` is planned to move to `archive/final_data_root_cleanup_20260705/data_archive`. No file moves, deletes, data mutation, cleanup/archive execution, provider calls, sidecar canonicalization, feature/WFA/prediction/modeling, commit, push, paper, or live work ran.
- Bounded path-level final data-root cleanup execution completed from the approved plan. Post-execution evidence: `reports/data_audit/current_state/final_data_root_cleanup_execution_plan_20260705/final_data_root_cleanup_execution_post_execution.{json,md}` with status `PASS_FINAL_DATA_ROOT_CLEANUP_EXECUTED`. Immediate prechecks passed: 168/168 label staging parquet files matched active `data/labeled`, 6/6 causal staging parquet files matched active `data/causally_gated_normalized`, 6/6 raw staging parquet files matched active `data/raw`, `data/dbn_sr_parent_candidate` had 0 files, and the archive destination was absent. Executed actions only deleted the three hash-matched staging roots, removed the empty `data/dbn_sr_parent_candidate`, and moved `data/archive` to `archive/final_data_root_cleanup_20260705/data_archive`. Top-level `data` now contains only `data/causally_gated_normalized`, `data/dbn`, `data/feature_matrices`, `data/labeled`, `data/predictions`, and `data/raw`; archive evidence is preserved outside `data` with 925 files. No provider calls, sidecar canonicalization, feature/WFA/prediction/modeling, commit, push, paper, or live work ran.
- Bounded report-only DBN-inner consolidation disposition completed for exactly six non-final direct children under `data/dbn`. Report: `reports/data_audit/current_state/dbn_inner_consolidation_disposition_20260705.{json,md}` with status `PASS_PLAN_READY_NO_EXECUTION`. It classifies `_promotion_staging` and `_archive_parent_consolidation` as `archive_evidence_candidate`, `sr1_sr3_2020_parent_sidecar_staging_20260705` as `protect_active_lineage`, and `ohlcv_1m_parent`, `statistics_parent`, and `status_parent` as `review_required_parent_source`. It recomputed file counts and tree hashes, verified KE staged targets match active canonical DBNs, verified KE archive files exist, verified SR staged sidecars remain active raw lineage dependencies, and approved 0 moves/deletes/data mutations.
- Bounded move-only DBN-inner cleanup executed for only `data/dbn/_promotion_staging` and `data/dbn/_archive_parent_consolidation`, preserving them under `archive/dbn_inner_consolidation_20260705/`. Execution report: `reports/data_audit/current_state/dbn_inner_consolidation_execution_20260705.{json,md}` with status `PASS_DBN_INNER_MOVE_EXECUTED`: 48 files moved, 24 DBN files, 24 manifests, file-level hashes preserved, and current direct `data/dbn` children exclude the two archived evidence roots. Post-move all-raw alignment artifact is `reports/data_audit/current_state/dbn_inner_consolidation_execution_20260705_all_raw_alignment.{json,md}` with status `PASS`, expected/raw 530/530, missing status/statistics 0/0, raw-only 0, source hash mismatches 0, and definition-join mismatches 0. The shell wrapper timed out after printing the PASS summary, so acceptance is based on direct JSON/Markdown artifact verification. No deletes, provider calls, sidecar canonicalization, parent-source promotion, raw/causal/label/feature/WFA/prediction/modeling, commit, push, paper, or live work ran.
- Bounded report-only DBN parent-source disposition refresh completed for only `data/dbn/ohlcv_1m_parent`, `data/dbn/statistics_parent`, and `data/dbn/status_parent`. Report: `reports/data_audit/current_state/dbn_parent_source_disposition_20260705.{json,md}` with status `PASS_PARENT_SOURCE_DISPOSITION_READY_NO_EXECUTION`: 126 parent DBN rows, 0 missing manifests, 2 `protect_active_lineage` rows for SR1/SR3 2020 OHLCV parent sources, 12 `already_promoted_or_excluded_from_parent_cleanup` KE rows whose canonical targets match parent hashes, and 112 `review_required_parent_source` rows. No moves, deletes, provider calls, sidecar canonicalization, parent-source promotion, downstream rebuilds, commits, pushes, paper, or live work ran.
- Bounded report-only DBN parent-source row-decision packet completed for the 112 `review_required_parent_source` rows from `reports/data_audit/current_state/dbn_parent_source_disposition_20260705.json`. Report: `reports/data_audit/current_state/dbn_parent_source_decision_packet_20260705.{json,md}` with status `PASS_PARENT_SOURCE_DECISION_PACKET_READY_NO_EXECUTION`: all 112 rows are `decision_required_no_action`, market counts are HE 45 / KE 22 / LE 45, root counts are `ohlcv_1m_parent` 38 / `statistics_parent` 37 / `status_parent` 37, and it excludes 2 protected SR active-lineage rows plus 12 already-promoted KE rows. No row is approved for promotion, archive, move, or deletion by this packet.
- Bounded move-only DBN parent-source row archive completed for the 112 archive-candidate rows from `reports/data_audit/current_state/dbn_parent_source_canonical_policy_20260705.json`. Executor: `scripts.validation.archive_dbn_parent_source_rows`; evidence: `reports/data_audit/current_state/dbn_parent_source_row_archive_20260705.{json,md}` with status `ARCHIVED`, 112 DBN rows / 224 files including manifests moved to `archive/dbn_parent_source_row_archive_20260705/data/dbn/...`. Bounded delete then removed only the 8 already-promoted KE statistics/status parent DBNs plus 8 manifests; evidence: `reports/data_audit/current_state/dbn_remaining_parent_source_delete_20260705.{json,md}` with status `DELETED`. `data/dbn/statistics_parent` and `data/dbn/status_parent` are absent; `data/dbn/ohlcv_1m_parent` remains with 6 DBNs / 6 manifests, and `data/dbn/sr1_sr3_2020_parent_sidecar_staging_20260705` remains with 4 DBNs / 4 manifests. No provider calls, promotions, sidecar canonicalization, downstream rebuilds, commits, pushes, paper, or live work ran.
- Bounded staged raw regeneration using the four local-only `download_databento_raw --mode convert-parquet` commands failed for KE 2019, 2021, 2023, and 2024 with `temporary parquet failed validation: ['duplicate_ts_event']`. The follow-up staged `scripts.validation.build_sr_front_contract_candidate` path succeeded for KE 2019/2021/2023/2024 without changing `data/raw/KE`: staged parquet now exists under `data/raw_parent_consolidation_staging/KE/`, reports are under `reports/data_audit/ke_parent_lineage_front_contract_raw_rebuild/`, and validation found 0 lineage/hash failures against canonical `data/dbn/.../KE/...` DBNs.
- SR1/SR3 2020 parent OHLCV-1m DBN source files now live under `data/dbn/ohlcv_1m_parent/{SR1,SR3}/2020/`; `data/dbn_sr_parent_candidate` has no remaining files, and active script/test/config references no longer point to `data/dbn_sr_parent_candidate`.
- Broad Phase 2 causal base parquet folders now live directly under `C:\Users\donny\Desktop\futures_intraday_model\data\causally_gated_normalized`; `data\causal_base_candidates` is absent after the move. Code/config references to active `broad_manifest_527_rebuild_v1` and archived `tier1_rebuild_v1` causal data roots now point to `data/causally_gated_normalized`; report roots that retain `broad_manifest_527_rebuild_v1` are historical/report labels only.
- The bulk alpha-discovery queue runner v1, proposal-only strategy-candidate ideation route, root ideation review-packet launcher, spec-driven dry-run config generator, Phase 9 wizard entry, optional discovery-run step, launcher self-check, and autopsy module are implemented and ready for review.
- `C:\Users\donny\Desktop\futures_intraday_model\RUN_ALPHA_DISCOVERY.bat` is the repo-local user-facing launcher and supports no-arg wizard mode, `--self-check`, proposal-only candidate ideation `--generate-candidates` without `--spec`, spec-driven dry-run config generation `--generate-candidates --spec <path>`, single-candidate `--config`, and serial queue `--queue` modes.
- `C:\Users\donny\Desktop\futures_intraday_model\RUN_STRATEGY_CANDIDATE_IDEATION.bat` is the root double-click launcher for proposal-only ideation review packets. With no arguments it writes up to 10 numbered v2 Markdown + JSON files directly under `reports/pipeline_audit/strategy_candidate_ideation/`, then prompts for a shortlist such as `1,3,7`, `all`, or `skip`. A successful selection writes only `reports/pipeline_audit/strategy_candidate_ideation/implementation_selection.json` with selected JSON paths and SHA-256 hashes for a later Codex implementation pass; it does not mutate registry/status, runnable harness specs, configs, data, logs, models, WFA, Phase 8, promotion, staging, commits, pushes, paper, or live execution.
- The repo-local `RUN_ALPHA_DISCOVERY.bat` is the only supported project `.bat` launcher; wizard self-check rejects non-repo-local launcher paths.
- `scripts.validation.run_alpha_discovery_queue` consumes already-created candidate-specific config JSON files; it does not generate, register, mutate, stage, commit, promote, paper trade, or live trade.
- `scripts.validation.generate_alpha_discovery_candidates` now has safe paths: without `--spec`, it prints target-only, horizon-agnostic, proposal-only Phase 9 strategy-candidate ideation with v2 draft-only JSON dossier metadata and no ES 30m harness dependency; with `--write-review-packet`, it writes short Markdown review cards plus JSON implementation handoff dossiers under ignored `reports/pipeline_audit/strategy_candidate_ideation/`; with `--write-review-packet --select-implementation`, it also prompts on stderr and writes/updates the ignored implementation shortlist under the resolved review root; with `--spec`, it consumes an explicit candidate spec, writes copied preflight-only candidate configs and one queue file under `configs/`, refuses overwrites, enforces max 100 candidates, and does not mutate registry/status, reports, data, logs, or models.
- Spec-driven candidate generation still requires canonical Phase 9 target-discovery readiness: each candidate must be a clean `CANDIDATE` in canonical target registry/status files and supported by the ES 30m target smoke harness `TARGET_SPECS`.
- 2026-07-05 repo-local wizard run stopped at `NO_CANONICAL_CANDIDATES`: no supported target currently has clean registry status `CANDIDATE` with latest trial stage `register_candidate`.
- Queue modes mirror the single-candidate runner: `preflight`, `source-tests`, `discovery-packet`, `discovery-run`, and `review`.
- `discovery-run` remains approval-bound: it requires `--approve-discovery-run`, the non-secret approval phrase `RUN_PHASE9_DISCOVERY_ONCE`, and `approved=true` on each queue entry.
- Queue discovery defaults to at most 10 candidates per invocation, has an absolute 100-candidate cap, runs serially, and records timeout as `STOP_INFRASTRUCTURE_TIMEOUT` with `RETRY_WITH_BOUNDED_INFRA_FIX`.
- Queue logs are ignored generated artifacts under `logs/alpha_discovery_queue/`; single-candidate logs remain under `logs/alpha_discovery/`.
- Wizard readiness autopsy writes ignored reports under `reports/pipeline_audit/alpha_discovery_autopsy/<batch>/readiness/` after generation/preflight and stamps reports `FEASIBILITY ONLY - NOT MODEL-TRUST EVIDENCE - DO NOT RETUNE FROM THIS AUTOPSY`.
- If the user explicitly chooses optional discovery-run, the wizard prints the exact repo-local launcher queue command, requires the fixed acknowledgement plus `RUN_PHASE9_DISCOVERY_ONCE`, writes a separate approved queue copy under `configs/`, runs bounded queue discovery, and writes post-discovery autopsy under `reports/pipeline_audit/alpha_discovery_autopsy/<batch>/discovery/`.
- Wrapper or queue completion is not candidate success. Candidate status must still be read from generated JSON when present.
- `DISCOVERY_PASS` is a candidate pass. Any `STOP_*` JSON decision is a candidate stop even if the subprocess exits `0`; missing JSON/MD outputs remain `DISCOVERY_RUN_REVIEW_REQUIRED`.
- Generated configs may still fail later runner preflight for stale outputs or missing generated-output ignore rules, but the generator now blocks unregistered, non-canonical, advanced, stopped, or unsupported target candidates before writing configs.
# Recent Changes
- 2026-07-06 Tier 1 core (`tier_1_core -> tier_1_research`, `ES`, `CL`, `ZN`, `6E`, years `2023`, `2024`) is active through Phase 4 from `reports/data_audit/current_state/tier1_core_phase34_active_placement_20260706/active_placement_report.{json,md}` and `reports/data_audit/current_state/tier1_core_feature_sidecar_active_placement_20260706/feature_sidecar_active_placement_report.{json,md}`. Phase 4 self-reference cleanup then changed only `scripts/phase4_features/build_baseline_features.py` and `tests/phase4_features/test_build_baseline_features.py`, removing the eight `feature_rel_ret_vs_{ES,ZN,CL,6E}_15` and `feature_corr_vs_{ES,ZN,CL,6E}_60` columns from trainable `FEATURE_COLS`; targeted Phase 4 tests passed, and staged rebuild evidence `reports/data_audit/current_state/tier1_core_phase4_self_reference_cleanup_implementation_20260706.{json,md}` is `PASS_TIER1_CORE_PHASE4_SELF_REFERENCE_CLEANUP_STAGED` with 8/8 staged files, feature count `114`, warnings `0`, failures `0`. Active `data/feature_matrices` was not replaced by the cleanup pass.
- 2026-07-05 moved 58 ignored 2025/2026 parquet files from `data/causal_proof_candidates/local_trade_2025_2026_v1/` into `data/causally_gated_normalized/`; source parquet count is now 0, canonical parquet count is now 518, all 58 expected paths exist, and schema mismatches vs latest prior canonical year are 0.
- 2026-07-05 updated relocation-sensitive validation code in `scripts/validation/build_local_trade_baseline_readiness.py` and `scripts/validation/project_local_trade_candidate_manifest_profile.py`, plus focused tests, so downstream label commands and projected manifests target the canonical causal root after the move while preserving candidate-root proof provenance.
- 2026-07-05 validation passed: `python -m pytest -q tests\validation\test_build_local_trade_model_eligible_scope.py tests\validation\test_build_local_trade_proof_status_promotion_proposal.py tests\validation\test_build_local_trade_baseline_readiness.py tests\validation\test_build_local_trade_upstream_manifest_evidence_resolver.py tests\validation\test_project_local_trade_candidate_manifest_profile.py`; `python -m scripts.validation.check_coordination_docs`.
- 2026-07-05 added `scripts/validation/consolidate_he_ke_le_dbn_parent_sources.py` and `tests/validation/test_consolidate_he_ke_le_dbn_parent_sources.py`; targeted validation passed with `python -m pytest -q tests\validation\test_consolidate_he_ke_le_dbn_parent_sources.py` (`7 passed`). The safe `decision-packet` mode wrote ignored generated reports under `reports/data_audit/` and did not mutate DBN, raw parquet, registry/status, staging, commits, pushes, paper, or live execution.
- 2026-07-05 created the KE-only approval JSON from the decision packet's `approval_template.approvals` and verified it with non-executing `python -m scripts.validation.consolidate_he_ke_le_dbn_parent_sources --mode stage-approved --approval-json reports/data_audit/he_ke_le_dbn_parent_consolidation_approval_ke_parent_lineage.json --timestamp dry_run_ke_parent_lineage_20260705`.
- 2026-07-05 executed bounded staging only: `python -m scripts.validation.consolidate_he_ke_le_dbn_parent_sources --mode stage-approved --approval-json reports/data_audit/he_ke_le_dbn_parent_consolidation_approval_ke_parent_lineage.json --timestamp ke_parent_lineage_20260705 --execute --report-out reports/data_audit/he_ke_le_dbn_parent_consolidation_stage_ke_parent_lineage.json`; follow-up staged-file validation checked 12 approval rows with 0 failures.
- 2026-07-05 executed bounded DBN promotion only: `python -m scripts.validation.consolidate_he_ke_le_dbn_parent_sources --mode promote-approved --approval-json reports/data_audit/he_ke_le_dbn_parent_consolidation_approval_ke_parent_lineage.json --staging-run-root data/dbn/_promotion_staging/ke_parent_lineage_20260705/canonical_layout --timestamp ke_parent_lineage_20260705 --execute --report-out reports/data_audit/he_ke_le_dbn_parent_consolidation_promote_ke_parent_lineage.json`; follow-up canonical/archive validation checked 12 approval rows with 0 failures.
- 2026-07-05 attempted bounded staged raw regeneration for KE 2019, 2021, 2023, and 2024 using the four local-only commands from `reports/data_audit/he_ke_le_dbn_parent_consolidation_promote_ke_parent_lineage.json`; all four launched and failed with `duplicate_ts_event` before writing staged parquet.
- 2026-07-05 inspected and ran a bounded staged front-contract candidate build for the promoted canonical parent-style KE DBNs: `python -m scripts.validation.build_sr_front_contract_candidate --candidate-dbn-root data/dbn/ohlcv_1m --sidecar-dbn-root data/dbn --definition-dbn-root data/dbn/definition --status-dbn-root data/dbn/status --statistics-dbn-root data/dbn/statistics --output-root data/raw_parent_consolidation_staging --reports-root reports/data_audit/ke_parent_lineage_front_contract_raw_rebuild --markets KE --years 2019 2021 2023 2024 --profile tier_3 --resolved-profile tier_3_research`. The command printed `PASS front-contract candidate build: outputs=4 failures=0`; the shell wrapper timed out after the PASS print, so success was accepted only after independent artifact validation.
- 2026-07-05 validated staged KE raw candidate parquet: `data/raw_parent_consolidation_staging/KE/{2019,2021,2023,2024}.parquet` exists; row counts are 94,469 / 107,460 / 102,470 / 110,139; each file has `market=KE`, the expected single `year`, 0 duplicate `ts_event` rows, report manifest status `PASS`, raw-alignment status `PASS`, and all `source_file`, `status_source_file`, and `stat_*_source_file` hashes match current canonical `data/dbn/ohlcv_1m`, `data/dbn/status`, and `data/dbn/statistics` KE DBNs.
- 2026-07-05 replaced only `data/raw/KE/{2019,2021,2023,2024}.parquet` from the validated staged raw files; wrote ignored report `reports/data_audit/ke_parent_lineage_raw_replacement_20260705.json` with status `PASS`, raw/staged hash matches, 0 duplicate `ts_event` rows, and canonical DBN lineage paths/hashes.
- 2026-07-05 generated and verified report-only post-replacement broad raw alignment: `python -m scripts.validation.audit_raw_dbn_alignment --profile all_raw --dbn-root data\dbn --raw-root data\raw --repair-manifest reports\data_audit\source_of_truth_lineage\broader_lineage_review_20260628\broad_manifest_527_rebuild_raw_alignment_repair_manifest.json --json-out reports\data_audit\current_state\post_replacement_all_raw_dbn_alignment_20260705.json --md-out reports\data_audit\current_state\post_replacement_all_raw_dbn_alignment_20260705.md`. The wrapper timed out after printing `status=PASS`, but direct JSON verification confirmed status `PASS`, expected/raw 530/530, source-hash mismatches 0, and definition-join mismatches 0.
- 2026-07-05 generated report-only SR1/SR3 2020 reconciliation: `reports/data_audit/current_state/sr1_sr3_2020_repair_source_reconciliation_20260705.{json,md}`. Current-lineage status is `FAIL`, archived-candidate trace status is `PASS`; SR1 raw rows 8,566 and SR3 raw rows 3,461; raw OHLCV source hashes match current `data/dbn/ohlcv_1m_parent`, but raw status/statistics hashes match archived `data/archive/.../dbn_sr_parent_candidate/status|statistics`, not current `data/dbn/status|statistics`.
- 2026-07-05 attempted bounded no-download staged raw regeneration: `python -m scripts.validation.build_sr_front_contract_candidate --candidate-dbn-root data\dbn\ohlcv_1m_parent --sidecar-dbn-root data\dbn --definition-dbn-root data\dbn\definition --status-dbn-root data\dbn\status --statistics-dbn-root data\dbn\statistics --output-root data\raw_parent_consolidation_staging\sr1_sr3_2020_current_sidecars_20260705 --reports-root reports\data_audit\sr1_sr3_2020_current_sidecar_raw_rebuild_20260705 --markets SR1 SR3 --years 2020 --profile all_raw --resolved-profile all_raw`. It returned `FAIL front-contract candidate build: outputs=0 failures=1`; no staged parquet was written. Source audit blockers are continuous-only current status/statistics sidecars for both SR1 and SR3.
- 2026-07-05 executed bounded zero-cost provider acquisition for SR1/SR3 2020 parent `status` and `statistics` sidecars only: dry-runs planned 4 total chunks, cost estimates returned `$0.0000` with 0 estimate errors, both zero-cost gates passed, four provider batch jobs reached `state=done`, and staged DBN SHA-256 values matched their sidecar manifests. Outputs are staged under `data/dbn/sr1_sr3_2020_parent_sidecar_staging_20260705/`; active canonical DBN/raw paths were not overwritten.
- 2026-07-05 executed bounded local staged raw regeneration for SR1/SR3 2020 with staged parent sidecars: `python -m scripts.validation.build_sr_front_contract_candidate --candidate-dbn-root data\dbn\ohlcv_1m_parent --definition-dbn-root data\dbn\definition --status-dbn-root data\dbn\sr1_sr3_2020_parent_sidecar_staging_20260705\status --statistics-dbn-root data\dbn\sr1_sr3_2020_parent_sidecar_staging_20260705\statistics --output-root data\raw_parent_consolidation_staging\sr1_sr3_2020_parent_sidecars_20260705 --reports-root reports\data_audit\sr1_sr3_2020_parent_sidecar_raw_rebuild_20260705 --markets SR1 SR3 --years 2020 --profile all_raw --resolved-profile all_raw`. It returned `PASS front-contract candidate build: outputs=2 failures=0`; direct checks confirmed report status `PASS`, raw-alignment status `PASS`, 0 source-hash mismatches, 0 duplicate `ts_event` rows, SR1 8,566 rows, SR3 3,461 rows, and output hashes `26ac930bc0443ebc07aa420aee3f9209c0ed085db71ebb2c22b18307a7ab276e` / `67452982a4d6a6d0ec5be90aee42e05346b867b7cb8094efa11bae6ca1a7d20b`. Active `data/raw` was not overwritten.
- 2026-07-05 replaced only `data/raw/SR1/2020.parquet` and `data/raw/SR3/2020.parquet` from `data/raw_parent_consolidation_staging/sr1_sr3_2020_parent_sidecars_20260705/{SR1,SR3}/2020.parquet`. Source audit passed (`reports/data_audit/sr1_sr3_2020_parent_sidecar_raw_replacement_source_audit_20260705.json`); replacement report passed (`reports/data_audit/sr1_sr3_2020_parent_sidecar_raw_replacement_20260705.json`) with active/staged/manifest hashes equal, 0 duplicate `ts_event` rows, SR1 8,566 rows, SR3 3,461 rows, and OHLCV/status/statistics lineage matching the expected parent sources. No DBN promotion, sidecar canonicalization, cleanup/archive, downstream causal/label/feature/WFA/prediction, staging, commit, push, paper, or live action ran.
- 2026-07-05 generated report-only current SR1/SR3 repair-source metadata at `reports/data_audit/current_state/post_sr_replacement_raw_alignment_repair_manifest_20260705.json` by preserving the four KE repair rows and replacing only the two SR entries with current `data/dbn/ohlcv_1m_parent` source paths from the SR replacement report. Then ran report-only `python -m scripts.validation.audit_raw_dbn_alignment --profile all_raw --dbn-root data\dbn --raw-root data\raw --repair-manifest reports\data_audit\current_state\post_sr_replacement_raw_alignment_repair_manifest_20260705.json --json-out reports\data_audit\current_state\post_sr_replacement_all_raw_dbn_alignment_20260705.json --md-out reports\data_audit\current_state\post_sr_replacement_all_raw_dbn_alignment_20260705.md`. The shell wrapper timed out after printing `status=PASS`; direct JSON/Markdown checks confirmed status `PASS`, expected/raw 530/530, source-hash mismatches 0, definition-join mismatches 0, repair manifest failures 0, and accepted repair sources 2 for SR1/SR3 2020.
- 2026-07-05 accepted the refreshed post-SR alignment plus SR replacement report as resolving the SR1/SR3 2020 caveat for data-consolidation purposes and generated no-mutation current-state ledger `reports/data_audit/current_state/data_current_state_20260705T153606Z.{json,md}`. Direct validation confirmed ledger `status=PASS`, one accepted decision, all_raw status `PASS`, expected/raw 530/530, source-hash mismatches 0, SR replacement status `PASS`, and four non-mutation blockers. No provider calls, data mutation, cleanup/archive, causal/label/feature/WFA/prediction/modeling, commit, push, paper, or live action ran.
- 2026-07-05 added and ran `scripts.validation.review_active_root_causal_readiness` as a bounded report-only active-root review of `data/raw -> data/causally_gated_normalized`. Output `reports/data_audit/current_state/active_root_raw_to_causal_readiness_20260705.{json,md}` returned `WARN`, with 0 active-causal hash mismatches vs historical output evidence, 0 causal-without-raw, 0 causal-without-evidence, 6 stale raw-input hash rows, and 12 raw-without-causal rows. No generated data changes or downstream work ran.
- 2026-07-05 added and ran `scripts.validation.plan_active_root_causal_rebuild_disposition` as a bounded report-only disposition plan from the approved current evidence. Output `reports/data_audit/current_state/active_root_causal_rebuild_disposition_plan_20260705.{json,md}` returned `PASS_PLAN_READY_NO_EXECUTION`, assigning `rebuild` to the six stale raw-input causal rows and `defer` to the twelve raw-without-causal rows. No generated data changes or downstream work ran.
- 2026-07-05 executed a bounded causal-only six-row rebuild via staged Phase 2 outputs and active causal copy-only replacement for `KE:2019`, `KE:2021`, `KE:2023`, `KE:2024`, `SR1:2020`, and `SR3:2020`. Validation passed: readiness `PASS` checked 6, build manifest `PASS` processed 6, active hashes match staged hashes for all six, and `reports/data_audit/current_state/active_root_six_causal_rebuild_validation_20260705.{json,md}` returned `PASS`.
- 2026-07-05 refreshed report-only active-root readiness and current-state ledger after the six-row causal rebuild. `scripts.validation.review_active_root_causal_readiness` now accepts the six-row rebuild manifest/validation evidence as an overriding causal evidence group. Output `reports/data_audit/current_state/active_root_raw_to_causal_readiness_post_six_causal_rebuild_20260705.{json,md}` returned `WARN` with stale raw-input hash rows 0, raw-without-causal rows 12, and severe blockers 0. Ledger `reports/data_audit/current_state/data_current_state_20260705T161444Z.{json,md}` returned `PASS` and records the same WARN caveats.
- 2026-07-05 updated and ran `scripts.validation.plan_active_root_causal_rebuild_disposition` for the post-six-row 12-row policy-disposition scope. Output `reports/data_audit/current_state/active_root_raw_without_causal_policy_disposition_20260705.{json,md}` returned `PASS_POLICY_DISPOSITION_READY_NO_EXECUTION`, assigning `defer` to all 12 raw-without-causal rows and assigning no `rebuild` or `exclude` rows. No generated data changes or downstream work ran.
- 2026-07-05 accepted the 12-row raw-without-causal `defer` disposition for current planning and generated report-only ledger `reports/data_audit/current_state/data_current_state_20260705T162319Z.{json,md}`. Direct validation confirmed ledger `status=PASS`, accepted decisions 2, `defer=12`, `rebuild=0`, `exclude=0`, readiness status `WARN`, stale raw-input hash rows 0, and raw-without-causal rows 12. No provider calls, data mutation, cleanup/archive, sidecar canonicalization, causal/label/feature/WFA/prediction/modeling, commit, push, paper, or live action ran.
- 2026-07-05 generated report-only cleanup/archive no-touch disposition `reports/data_audit/current_state/cleanup_archive_no_touch_disposition_20260705.{json,md}`. Direct validation confirmed status `PASS_NO_TOUCH_PLAN_READY_NO_EXECUTION`, 12 scoped roots, 6 `protect_no_touch`, 2 `review_required_no_touch`, 4 `absent_no_action`, and 0 approved file moves, deletes, or data mutations. No provider calls, data mutation, cleanup/archive execution, sidecar canonicalization, labels/features/WFA/predictions/modeling, commit, push, paper, or live action ran.
- 2026-07-05 accepted the cleanup/archive no-touch disposition as report-only planning evidence and generated downstream causal-to-label readiness report `reports/data_audit/current_state/active_root_causal_to_label_readiness_20260705.{json,md}`. Direct validation confirmed status `BLOCKED_NOT_READY_NO_EXECUTION`, active causal/label/matched counts 518/405/404, causal-without-active-label 114, active-label-without-active-causal 1, matched row-count mismatches 54, nested non-active label files 8, and current PASS full active-root causal manifest count 0. No label files or other data were written.
- 2026-07-05 generated report-only label rebuild/disposition plan `reports/data_audit/current_state/active_root_label_rebuild_disposition_plan_20260705.{json,md}`. Direct validation confirmed status `PASS_PLAN_READY_NO_EXECUTION`, execution readiness `BLOCKED_PENDING_ACTIVE_CAUSAL_MANIFEST_GATE`, one manifest-gate precondition row, 169 scoped label issue rows, 168 `rebuild_label_after_manifest_gate`, 1 `exclude_from_active_label_scope_no_touch`, 0 defers, and 0 input-scope failures. No label files or other data were written.
- 2026-07-05 generated report-only active causal manifest gate evidence under `reports/data_audit/current_state/active_root_causal_manifest_gate_20260705/`. Direct validation confirmed `causal_base_manifest.json` status `PASS`, `active_causal_manifest_gate_report.json` status `PASS_MANIFEST_GATE_READY_NO_EXECUTION`, Phase 3 manifest-gate check `PASS`, 518 active causal pairs, 153,213,286 total rows, and 0 gate failures. No label files or other data were written.
- 2026-07-05 executed bounded staged label rebuild for exactly 168 approved rows into `data/labeled_rebuild_staging/active_root_label_rebuild_20260705/` using `reports/data_audit/current_state/active_root_causal_manifest_gate_20260705/causal_base_manifest.json`. Validation `reports/data_audit/current_state/active_root_label_rebuild_staging_20260705/staged_label_rebuild_validation.{json,md}` returned `PASS_STAGED_LABEL_REBUILD_READY_NO_ACTIVE_REPLACEMENT`: staged parquet count 168, label report/manifest `PASS`, input/output rows 43,057,061, active label hash changes 0, validation failures 0, and no staged `6M:2012.parquet`. Active `data/labeled` was not overwritten.
- 2026-07-05 fixed `scripts.validation.consolidate_he_ke_le_dbn_parent_sources` validation so all-null optional statistic source columns are skipped while at least one populated canonical statistics lineage column is still required. Validation passed: `python -m pytest -q tests\validation\test_consolidate_he_ke_le_dbn_parent_sources.py` (`8 passed`); `python -m scripts.validation.consolidate_he_ke_le_dbn_parent_sources --mode validate --approval-json reports/data_audit/he_ke_le_dbn_parent_consolidation_approval_ke_parent_lineage.json --report-out reports/data_audit/he_ke_le_dbn_parent_consolidation_validate_after_raw_replacement.json` (`PASS`).
- 2026-07-05 moved exactly four SR parent source files from `data/dbn_sr_parent_candidate/{SR1,SR3}/2020/` into `data/dbn/ohlcv_1m_parent/{SR1,SR3}/2020/`, updated their manifest `path` fields, redirected active validation code/tests to `data/dbn/ohlcv_1m_parent`, hash-verified SR1 `4ef063289d591e6aeeb4ee646afd7f6153a747ccdda6ddfd8841690fe127dd91` and SR3 `74ab12437e9a0b781bc09a2dca892ff63be3d3007eef0e15fef0916152ef2f80`, and validated with `python -m pytest -q tests\validation\test_dispose_broad_causal_source_reference_failures.py tests\validation\test_record_broad_causal_source_artifact_policy.py tests\validation\test_record_broad_causal_source_artifact_policy_selection.py tests\validation\test_request_broad_causal_source_artifact_policy_decision.py tests\validation\test_summarize_broad_causal_rebuild_gate.py` (`33 passed`).
- 2026-07-05 moved 33 market folders / 460 parquet files from `data/causal_base_candidates/broad_manifest_527_rebuild_v1` into `data/causally_gated_normalized`, redirected active causal-root code/config/test references to the canonical root, and validated with `python -m pytest -q tests\phase2_causal_base\test_build_causal_base_data.py tests\phase3_labels\test_build_labels.py tests\validation\test_build_broad_manifest_527_rebuild.py tests\validation\test_build_holdout_forward_evidence.py tests\validation\test_audit_tier_research_ladder.py tests\validation\test_check_phase2_460_audit_readiness.py tests\validation\test_evaluate_phase2_canonical_promotion_gate.py tests\validation\test_plan_broad_causal_rebuild.py tests\validation\test_refresh_master_data_health_matrix.py tests\validation\test_validate_broad_causal_raw_source_readiness.py` (`238 passed`).
- 2026-07-05 added `scripts/validation/run_alpha_discovery_queue.py`, `configs/alpha_discovery_queue.example.json`, `.bat` queue routing, queue tests, and a `PROJECT_OUTLINE.md` queue-runner note.
- 2026-07-05 consolidated the user-facing launcher at `C:\Users\donny\Desktop\futures_intraday_model\RUN_ALPHA_DISCOVERY.bat`; the Desktop-root copy is no longer the supported project launcher.
- 2026-07-05 added `scripts/validation/generate_alpha_discovery_candidates.py`, `configs/alpha_discovery_candidates.example.json`, generator tests, repo-local `.bat` generator routing, and a `PROJECT_OUTLINE.md` generator note.
- 2026-07-05 changed the no-spec `--generate-candidates` route into proposal-only Phase 9 strategy-candidate ideation; the old config/queue artifact generator remains available with `--spec` and for wizard internals.
- 2026-07-05 added `RUN_STRATEGY_CANDIDATE_IDEATION.bat`, `--write-review-packet`, v2 short Markdown review cards, draft-only JSON implementation dossiers, marker-safe archive/overwrite behavior, direct numbered Markdown/JSON candidate file writing, and ideation launcher self-check coverage.
- 2026-07-05 tightened v2 ideation packets: `CANDIDATE_PACKET_FILENAME_RE` validates packet filenames, v1 or unmarked current matching files are archived before new writes, v2-marked generated files may be overwritten on rerun, `registry_patch_draft` / `trial_status_patch_draft` replace old draft-entry field names, `post_registration_config_spec_draft.runnable` is `false`, and generated JSON separates `wizard_readiness_status` from `evidence_status`.
- 2026-07-05 tightened the generator with a canonical Phase 9 target-discovery gate tied to `manifests/target_hypotheses/*` and the ES 30m smoke harness target specs.
- 2026-07-05 added repo-owned `RUN_ALPHA_DISCOVERY.bat`, `scripts.validation.run_alpha_discovery_wizard`, `scripts.validation.alpha_discovery_autopsy`, static launcher self-check, safe autopsy wording, bounded queue discovery caps, and child-process-tree timeout cleanup.
- 2026-07-05 completed the wizard optional discovery-run branch: exact command display, fixed acknowledgement and approval phrase, approved queue copy, bounded queue invocation, and post-discovery autopsy.
- 2026-07-05 targeted validation passed: `python -m pytest -q tests\validation\test_alpha_discovery_wizard.py tests\validation\test_alpha_discovery_autopsy.py tests\validation\test_run_alpha_discovery_queue.py tests\validation\test_run_alpha_discovery.py tests\validation\test_generate_alpha_discovery_candidates.py`.
- 2026-07-05 targeted validation passed: `python -m pytest -q tests\validation\test_run_alpha_discovery.py tests\validation\test_run_alpha_discovery_queue.py`.
- 2026-07-05 v2 ideation packet validation passed: `python -m pytest tests\validation\test_generate_alpha_discovery_candidates.py tests\validation\test_alpha_discovery_wizard.py tests\validation\test_run_alpha_discovery.py -q`; `python -m scripts.validation.check_coordination_docs`; launcher smoke `.\RUN_STRATEGY_CANDIDATE_IDEATION.bat --max-ideas 5 --timestamp codex_v2_smoke_20260705T000005Z`; before/after `scripts/phase9_research/es_30m_target_smoke_harness.py` hash `1C139DE90EF9C4128A334C942FAC737F83880E46FCD23EF0395CE8A96E9306E5`; `git diff --check`.
- The v2 launcher smoke archived the previous unmarked numbered packet files under `reports/pipeline_audit/strategy_candidate_ideation/_archive/codex_v2_smoke_20260705T000005Z/` and wrote five current v2 Markdown/JSON pairs directly under `reports/pipeline_audit/strategy_candidate_ideation/`.
- 2026-07-05 compacted this handoff so active state is short, current, and checker-enforced.
- 2026-07-05 hardened `scripts/validation/check_coordination_docs.py` to cap active handoff length and reject duplicate or stale continuation markers.
- 2026-07-05 preserved the current `README.md` rewrite while restoring the checker-compatible `PROJECT_OUTLINE.md` / `PIPELINE.md` source-of-truth sentence.
- 2026-07-05 ran `cmd.exe /d /c "echo ACK|RUN_ALPHA_DISCOVERY.bat"`; launcher self-check passed and wizard returned `NO_CANONICAL_CANDIDATES` without writing candidate configs, reports, logs, registry/status changes, staging, commits, or pushes.
- 2026-07-05 expanded the proposal-only ideation library/default launcher packet from 5 to 10 ideas by adding draft-only candidates for VWAP reclaim continuation, gap-fill failure continuation, opening-drive failed follow-through, inside-session range breakout, and volume-pace breakout continuation.
- 2026-07-05 validation passed for the expanded ideation route: `python -m pytest -q tests\validation\test_generate_alpha_discovery_candidates.py`; `python -m scripts.validation.generate_alpha_discovery_candidates --self-check --launcher-path RUN_STRATEGY_CANDIDATE_IDEATION.bat`.
- 2026-07-05 generated a bounded 10-card proposal-only packet with `.\RUN_STRATEGY_CANDIDATE_IDEATION.bat --max-ideas 10 --timestamp codex_10_ideas_20260705T000000Z`; it wrote 10 Markdown/JSON pairs under `reports/pipeline_audit/strategy_candidate_ideation/`, moved no archive files, and did not mutate registry/status, `TARGET_SPECS`, configs, data, logs, models, WFA, Phase 8, staging, commits, pushes, paper, or live execution.
- 2026-07-05 added implementation shortlist selection to the ideation workflow: no-arg `RUN_STRATEGY_CANDIDATE_IDEATION.bat` now includes `--select-implementation`, stdout remains machine-readable JSON, prompts/summaries go to stderr, and selections write ignored `implementation_selection.json` with on-disk JSON SHA-256 hashes under the review root.
- 2026-07-05 refactored proposal-only ideation to be target-only and horizon-agnostic: the default packet uses 5m, 15m, 30m, 60m, first-touch, and session-close target ideas, includes `horizon_label`, `target_horizon`, `exit_rule`, `conversion_required: true`, and `current_wizard_compatible: false`, and lazy-loads ES 30m `TARGET_SPECS` only for strict `--spec` config generation.
- 2026-07-05 generated a bounded horizon-agnostic 10-card proposal-only packet with `.\RUN_STRATEGY_CANDIDATE_IDEATION.bat --max-ideas 10 --timestamp codex_horizon_agnostic_20260705T000000Z`; it wrote 10 Markdown/JSON pairs under `reports/pipeline_audit/strategy_candidate_ideation/`, moved no archive files, and did not mutate registry/status, runnable harness specs, configs, data, logs, models, WFA, Phase 8, staging, commits, pushes, paper, or live execution.
- 2026-07-06 refactored `PROJECT_OUTLINE.md` as a single-file outline/runbook cleanup: centralized shared gate evidence format, shortened repeated downstream blockers, compacted Phase 9 launcher wording, and condensed the historical Tier 1 status appendix while preserving coordination-checker and doc-test contract strings. No code, config, data, reports, schemas, CLI behavior, provider calls, WFA/modeling, commits, pushes, paper, or live work ran. Older detailed run chronology remains intentionally removed from active handoff text; use git history only if exact historical command text is needed.
# Active Tasks
- The approved KE-only DBN consolidation has completed DBN promotion and raw parquet replacement for KE 2019/2021/2023/2024. Remaining parent-source cleanup has archived/deleted only bounded approved rows; keep `data/dbn/ohlcv_1m_parent` and the SR sidecar staging root unless a later explicit bounded decision says otherwise.
- Use `reports/data_audit/current_state/data_current_state_20260705T162319Z.{json,md}` as the latest current-state ledger for data-consolidation planning. The staged parent `status`/`statistics` sidecar root is active raw lineage evidence and must not be deleted, moved, archived, or canonicalized without a separate explicit bounded decision.
- Use `reports/data_audit/current_state/dbn_inner_consolidation_execution_20260705.{json,md}` as the current DBN-inner cleanup execution evidence. `_promotion_staging` and `_archive_parent_consolidation` are preserved outside `data/dbn` under `archive/dbn_inner_consolidation_20260705/`; `statistics_parent` and `status_parent` are now absent after bounded delete, and `ohlcv_1m_parent` plus the SR sidecar staging root remain protected.
- Use `reports/data_audit/current_state/dbn_parent_source_row_archive_20260705.{json,md}` and `reports/data_audit/current_state/dbn_remaining_parent_source_delete_20260705.{json,md}` as current parent-source cleanup evidence. `data/dbn/statistics_parent` and `data/dbn/status_parent` should be absent; `data/dbn/ohlcv_1m_parent` should remain with 6 DBNs / 6 manifests, including the 2 protected SR1/SR3 2020 OHLCV rows; do not move/delete/canonicalize it or the SR sidecar staging root without a separate bounded decision.
- Use `reports/data_audit/current_state/dbn_parent_source_disposition_20260705.{json,md}`, `reports/data_audit/current_state/dbn_parent_source_decision_packet_20260705.{json,md}`, and `reports/data_audit/current_state/dbn_parent_source_canonical_policy_20260705.{json,md}` as historical row-selection evidence for the executed archive. They explain why 106 rows were canonical Phase 2 lineage archive candidates and six status rows (`HE:2013`, `KE:2014`, `LE:2010`, `LE:2011`, `LE:2012`, `LE:2013`) were archive candidates after decoded parent-vs-canonical status records matched when sorted.
- 2026-07-05 read-only smoke testing against the screenshot DBN audit checklist passed for the non-six scoped HE/KE/LE rows: current evidence has 38 policy market-years, excluding the six status-equality rows leaves 32 rows, consisting of 31 canonical-status rows plus the `KE:2013` provider-empty status exception. The 31 normal status manifests/files passed checklist-key, request-parameter, file-size, and SHA-256 checks; the all-raw alignment report remains `PASS` with 0 source-hash mismatches, 0 invalid manifests, 0 raw schema failures, and 0 definition-join mismatches; the active causal manifest gate remains `PASS_MANIFEST_GATE_READY_NO_EXECUTION` with 0 gate failures. Optional `split_symbols` is absent on `KE:2015`, `KE:2016`, `KE:2017`, `KE:2018`, `KE:2020`, and `KE:2022`; this is metadata-shape drift, not a checklist failure, because the audited fields and hashes pass.
- `PROJECT_OUTLINE.md` and `configs/data_manifest.yaml` now record the audit-ready DBN/raw/causal phase structure: Phase 1A requires a dry-run/request-plan command before provider work, then cost estimate evidence or an explicit zero-cost gate when provider metadata calls are approved, and only then a bounded provider-download command after acceptance; Phase 1B is incomplete unless both raw parquet conversion and raw DBN/parquet validation pass for the same declared scope; Phase 2 is incomplete unless readiness-only, causal build, and generated causal manifest/validation evidence all pass for the same declared scope. The historical `scripts.phase1C_validate.audit_raw_dbn_alignment` command remains documented as a legacy/internal Phase 1B validator path, not a separate public pipeline phase. The common smoke compile command includes `scripts\phase1C_validate\audit_raw_dbn_alignment.py`. This was a documentation/config-comment update only; no pipeline code, provider calls, downloads, data replacement, or raw/causal rebuilds ran.
- `PROJECT_OUTLINE.md` now records DBN `stype_in` policy explicitly: `definition` DBNs are the approved `stype_in=parent` exception and should not be redownloaded only because a parent-stype audit flags them; canonical `ohlcv-1m`, `status`, and `statistics` should be `stype_in=continuous` unless a row-specific exception is approved. Bounded 79-row non-definition parent-stype evidence is under `reports/data_audit/dbn_non_definition_parent_redownload_20260706T022731Z/`: provider cost gate estimated `$0.0000`; staged all-79 attempt validated 8 canonical staged continuous DBNs/manifests (`KE` `ohlcv-1m` and `status` for 2019/2021/2023/2024), 67 older status rows returned Databento `422 data_no_data_found_for_request`, and 4 `KE` statistics rows remain unresolved because batch jobs reached `done` but the client could not find the expected `.statistics.dbn.zst` file. Active placement completed for exactly those 8 validated staged DBNs/manifests: `active_placement_8_validated_report.{json,md}` status `PASS_ACTIVE_PLACEMENT_8_VALIDATED`, snapshot files 16/16 hash-pass under `archive/dbn_non_definition_parent_redownload_20260706T022731Z/active_replacement_snapshot/`, active verification 8/8 PASS, manifests rewrote only `path` to active `data/dbn`, and one-time downstream raw/causal source-hash mismatches for exactly `KE` 2019/2021/2023/2024 `ohlcv_1m` and `status` are classified only as `expected_pending_phase1b_rebuild` until approved rebuilds run; DBN file-byte SHA, file size, schema, market, year, stype, symbol, and active manifest path validation are not waived. No statistics replacement, raw/causal rebuilds, commits, pushes, paper, or live work ran.
- Seven-row HE/KE/LE status redownload preflight passed for `HE:2013`, `KE:2013`, `KE:2014`, `LE:2010`, `LE:2011`, `LE:2012`, and `LE:2013` only. Evidence: dry-run plans under `reports/data_audit/status_redownload_20260706T7row_preflight_v1/`. Direct checks found packet scope failures 0, active status DBNs/manifests missing 7/7 as expected after user deletion, active raw and causal parquet snapshot inputs present 7/7 and SHA-256 hashable, dry-run plans 7/7 valid with one `schema=status`, `stype_in=continuous`, `stype_out=instrument_id`, `dataset=GLBX.MDP3` task each, and staged output paths only under `archive/status_redownload_20260706T7row_preflight_v1/staged_dbn/status/...`. Cost/zero-cost gate evidence under `reports/data_audit/status_redownload_20260706T7row_cost_gate_v1/HE_2013/` stopped on the first row as required: `HE:2013` returned estimate status `ok`, cost `$0.0000`, billable size 0, provider-empty count 1, downloadable zero-cost tasks 0, and gate status `FAIL`. Remaining six rows were not estimated. No provider download, archive root creation, active replacement, raw rebuild, causal rebuild, cleanup, commit, push, paper, or live work ran.
- SR1/SR3 2020 staged DBN download, staged-only validation, and bounded copy-only active DBN placement completed. Evidence: preflight plans under `reports/data_audit/sr1_sr3_2020_four_schema_preflight_v1/`, cost gates under `reports/data_audit/sr1_sr3_2020_four_schema_cost_gate_v1/`, staged-download reports under `reports/data_audit/sr1_sr3_2020_four_schema_staged_download_v1/`, definition resume reports under `reports/data_audit/sr1_sr3_2020_definition_resume_v1/`, staged validation v2 under `reports/data_audit/sr1_sr3_2020_four_schema_staged_validation_v2/`, and active placement validation v2 under `reports/data_audit/sr1_sr3_2020_four_schema_active_placement_v2/`. Active placement copied exactly 8 DBNs/manifests into `data/dbn/{ohlcv_1m,status,statistics,definition}/{SR1,SR3}/2020/`, rewrote only manifest `path` fields to active paths, and passed hash, size, manifest-request, decoded row/index/timestamp, and normalized-column checks 8/8. Warning count 2 is limited to accepted definition pre-start `ts_event` metadata because `ts_recv` is inside the request window. The staged files and preserved definition temp directory remain untouched. The earlier `active_placement_v1` report failed only from an over-strict decoded-column comparator and is superseded by v2. No provider download, raw/causal rebuild, cleanup, commit, push, paper, or live work ran.
- Bounded report-only SR1/SR3 2020 active raw/DBN alignment refresh and user-approved source-hash exception completed under `reports/data_audit/current_state/sr1_sr3_2020_active_dbn_raw_alignment_refresh_v1/`. No-exception alignment is preserved as `raw_dbn_alignment_no_exception_fail.{json,md}` with two source-hash mismatches: current active raw source hashes match archived `archive/ohlcv_1m_parent` SR1/SR3 2020 DBNs, not newly placed active `data/dbn/ohlcv_1m`, and active `data/dbn/ohlcv_1m_parent` is absent. Exception manifest `sr1_sr3_2020_source_hash_exception_manifest.{json,md}` is limited to SR1/SR3 2020 only and validates row count plus raw `source_file`. Exception-backed alignment `raw_dbn_alignment_with_exception.{json,md}` is `PASS`: accepted repair sources 2, source-hash mismatches 0, invalid manifests 0, raw schema failures 0, definition-join mismatches 0. Downstream gate summary is `PASS_DOWNSTREAM_HASH_EXCEPTION_READY`; use the exception-backed PASS report for future SR1/SR3 2020 downstream gates so this approved hash mismatch alone does not fail the pipeline. No provider download, DBN placement, raw rebuild, active raw replacement, causal rebuild, cleanup, commit, push, paper, or live work ran.
- Bounded report-only Phase 1A/1B exact 33-market 2010-2026 preflight completed under `reports/data_audit/current_state/phase1ab_33markets_2010_2026_preflight_20260706/`. Current-horizon Phase 1A uses `--end-date 2026-06-13` to match active partial 2026 DBN chunks and returns `FAIL`: expected archives 2,108, missing archives/manifests 6/6, invalid manifests 0, required exception failures 0. Blocking rows are all `status` DBNs: `HE:2013`, `KE:2014`, and `LE:2010-2013`; `KE:2013` remains covered by the provider-empty required-schema exception. Strict full-year-2026 diagnostic also exists and fails with 138 missing rows because it expects full `2026-01-01_2027-01-01` archives while active 2026 chunks end `2026-06-13`. Phase 1B raw/DBN alignment is `FAIL` only from the same six missing status rows producing six raw-only rows; expected/raw 527/527, needs Phase 1B conversion 0, invalid manifests 0, raw schema failures 0, source-hash mismatches 0, definition-join mismatches 0. The SR1/SR3 2020 approved source-hash exception was applied and accepted for two rows. This pre-placement FAIL is historical/superseded for current audit scoring by the post-status-placement PASS refresh below. No provider download, DBN placement, raw rebuild, causal rebuild, cleanup, commit, push, paper, or live work ran.
- Bounded copy-only active status placement completed for exactly `HE:2013`, `KE:2014`, and `LE:2010-2013`. Readiness evidence is `reports/data_audit/current_state/phase1ab_archive_copy_readiness_20260706/archive_copy_readiness_review.{json,md}`; placement evidence is `reports/data_audit/current_state/phase1ab_archive_copy_active_status_placement_20260706/active_status_placement_report.{json,md}` with `PASS_ACTIVE_STATUS_PLACEMENT_WITH_PARENT_EXCEPTIONS`. Follow-up no-overwrite verification is `reports/data_audit/current_state/phase1ab_archive_copy_readiness_20260706/active_status_copy_placement_report.{json,md}` with `PASS_ACTIVE_PARENT_STATUS_EXCEPTION_ALREADY_PRESENT_VERIFIED`: active targets were already present, matched archive DBN hashes 6/6, had active `data/dbn/status/...` manifest paths, and no additional overwrite copy was performed. These remain explicit parent-status exceptions with `stype_in=parent`. No provider download, raw rebuild, causal rebuild, cleanup, commit, push, paper, or live work ran.
- Bounded report-only Phase 1A/1B current-horizon refresh after the six-row active status placement completed under `reports/data_audit/current_state/phase1ab_33markets_2010_2026_post_status_placement_refresh_20260706_rerun1/`. Summary `phase1ab_post_status_placement_refresh_summary.{json,md}` is `PASS`: Phase 1A expected archives 2,108 with 0 missing archives, 0 missing manifests, 0 invalid manifests, and 1 clean required-schema exception; Phase 1B was rerun as 33 one-market expected-only batches after stopping a stuck full-scope validator, with 33/33 PASS, expected/raw 527/527, needs Phase 1B conversion 0, raw-only 0, invalid manifests 0, source-hash mismatches 0, definition-join mismatches 0, raw schema/value failures 0, required-schema exception failures 0, and 6 accepted repair sources. Follow-on bounded report-only Phase 2 readiness/current-state refresh completed under `reports/data_audit/current_state/phase2_readiness_current_state_post_phase1ab_refresh_20260706/`: current-state ledger `data_current_state_post_phase1ab_phase2_refresh_20260706.{json,md}` is `PASS`, readiness is `WARN`, active raw/causal/evidence pairs are 530/518/518, stale raw-input hash rows 0, active causal hash/row mismatches 0/0, raw-without-causal 12, severe blockers 0, and medium blockers 2. Bounded staged-only holdout/forward Phase 2 run for `6E/CL/ES/ZN` 2025/2026 wrote summary `reports/data_audit/current_state/holdout_forward_8_phase2_staged_20260706/staged_phase2_holdout_forward_summary.{json,md}` with `PARTIAL_PASS_2025_BLOCKED_2026_NO_ACTIVE_REPLACEMENT`: 2025 holdout readiness/build/validation passed for 4 staged parquet files under `data/causally_gated_normalized_rebuild_staging/holdout_forward_8_phase2_staged_20260706/`; 2026 forward stopped before build because ES 2026 readiness failed on a degraded-row warning while the configured exception is stale/unmatched; active `data/causally_gated_normalized` was not changed. Follow-up ES-vs-NQ 2026 phase-chain diagnostic `reports/data_audit/current_state/es_nq_2026_phase_chain_diagnostic_20260706.{json,md}` is `PASS_DIAGNOSTIC_NO_MUTATION`: all scoped ES/NQ DBN manifests/files passed Phase 1A checks, both raw parquet files exist, ES and NQ raw degraded-row rates are nearly identical (1.74227% vs 1.73952%), and the likely issue is current Phase 2 readiness policy/exception mismatch rather than an ES-only bad DBN/raw conversion. No provider download, DBN placement, raw rebuild, active causal replacement, labels, features, WFA, cleanup, commit, push, paper, or live work ran.
- Bounded report-only ES/NQ 2026 degraded-session drilldown completed under `reports/data_audit/current_state/es_nq_2026_degraded_session_drilldown_20260706/` from `scripts.validation.drilldown_es_nq_2026_degraded_sessions`. Status is `PASS_DIAGNOSTIC_NO_MUTATION`: ES and NQ raw degraded session dates match, raw degraded rates remain nearly identical, and the exception review call is `SCOPED_EXCEPTION_REVIEW_SUPPORTED_NOT_APPROVED`. This is diagnostic evidence only; it does not approve a scoped readiness exception, Phase 2 rebuild, active replacement, labels/features/WFA/predictions, paper, or live work.
- User-approved historical-scope exclusion for exactly `6M:2012` and `TN:2010-2012` from active causal scope is recorded at `reports/data_audit/current_state/active_causal_scope_exclusions_historical_6m_tn_20260706.{json,md}` with no provider calls, no raw deletion, no data mutation, and no causal rebuild approval. `scripts.validation.review_active_root_causal_readiness` now accepts a validated `--approved-exclusions` report. Refreshed readiness is `reports/data_audit/current_state/phase2_readiness_post_historical_6m_tn_exclusions_20260706/active_root_raw_to_causal_readiness_post_historical_6m_tn_exclusions.{json,md}` with status `WARN`: active raw 530, active-scope raw after approved exclusions 526, active causal 518, historical evidence 518, approved active-scope exclusions 4, raw-without-causal all rows 12, unapproved raw-without-causal rows 8 (`6E`, `CL`, `ES`, and `ZN` 2025/2026 only), stale raw-input hash rows 0, active causal hash/row mismatches 0/0, causal-without-evidence 0, severe blockers 0, and medium blockers 2. No provider download, raw deletion, data mutation, DBN placement, raw rebuild, causal rebuild, cleanup, commit, push, paper, or live work ran.
- Treat `reports/data_audit/current_state/post_label_replacement_causal_to_label_alignment_20260705.{json,md}`, `reports/data_audit/current_state/active_root_label_replacement_20260705/active_label_replacement_report.{json,md}`, and `reports/data_audit/current_state/active_root_label_manifest_gate_20260705/` as pre-reset historical label evidence only. They explain the prior label state before the archive reset, but they are not current active label readiness evidence after active `data/labeled` was emptied.
- Treat `reports/features_baseline/active_root_baseline_features_20260705/phase4_staged_feature_build_blocker.{json,md}` as historical Phase 4 root-cause evidence only. The old 336-failure verdict is superseded for Tier 1 core by the cleaned 114-feature active placement below.
- Use `reports/data_audit/current_state/phase3_phase4_archive_reset_plan_20260705/phase3_phase4_archive_reset_execution_post.{json,md}` as historical reset evidence. It predates the later Tier 1 core label/feature rebuild and cleaned active feature placement.
- Use `reports/data_audit/current_state/nested_tier1_label_disposition_20260705/nested_tier1_label_move_post_execution.{json,md}` as the current evidence for the nested Tier1 label cleanup: `data/labeled/tier1_rebuild_v1` has been moved outside `data`, and active direct label files remain manifest-matching.
- Bounded active placement completed for the cleaned Tier 1 core Phase 4 self-reference cleanup outputs. Evidence: `reports/data_audit/current_state/tier1_core_phase4_self_reference_cleanup_active_placement_20260706/active_placement_report.{json,md}` with `PASS_TIER1_CORE_PHASE4_SELF_REFERENCE_CLEANUP_ACTIVE_PLACEMENT`: 8 feature parquets plus 4 sidecars placed into `data/feature_matrices`, active feature count 114, removed self-reference features present 0, and all 12 placed destination hashes verified. Added report-only `python -m scripts.validation.audit_model_trust`; current evidence under `reports/model_trust_audit/codex_model_trust_audit_20260706T000000Z/` returns `permitted_use_label=research_only` with 58 metrics scored 0/inadmissible until exact-scope trial, prediction, costed OOS, promotion, and paper/live evidence exists. No WFA/modeling/predictions, provider calls, DBN/raw/causal/label writes, commits, pushes, paper/live work, or cleanup ran.
- Bounded active Tier 1 Phase 5 WFA preflight, split-plan generation, split-plan acceptance review, Phase 6/WFA runner preflight, ES-only smoke execution, and full Phase 6 report-only WFA execution completed for `ES,CL,ZN,6E` years `2023,2024`. Phase 5/preflight/split evidence remains `PASS`: `reports/data_audit/current_state/tier1_core_phase5_wfa_preflight_20260706/phase5_wfa_preflight_report.{json,md}`, `reports/wfa/tier1_core_phase5_split_plan_20260706/split_plan.{json,csv}`, and `split_plan_acceptance_report.{json,md}` show 8 active feature parquets, 114 active features, 48 folds, 12 folds per market, feature-manifest gate hash PASS, data-audit universe hash/usable rows PASS, generated-artifact scope PASS, and prediction files 0. Phase 6 preflight evidence: `reports/wfa/tier1_core_phase6_wfa_runner_preflight_20260706/wfa_runner_preflight_report.{json,md}` with `PASS_PHASE6_WFA_RUNNER_PREFLIGHT_READY_REPORT_ONLY`; report-local `tier1_core_active_feature_set.json` is `FROZEN`, `allowed_for_wfa=true`, and has 114 features. ES-only smoke evidence remains PASS under `reports/wfa/tier1_core_phase6_smoke_es1_report_only_20260706/`. Full report-only evidence: `reports/wfa/tier1_core_phase6_full_report_only_20260706/tier1_core_phase6_full_report_only_20260706_wfa_report.json` and `_predictions_manifest.json` from `python -m scripts.phase6_wfa.run_wfa` over all 48 accepted research folds with no `--write-predictions` and no `--predictions-root`. Status `PASS`, failure_count 0, warning_count 0, fold_count 48, diagnostics 384 all PASS, 8 models, in-memory prediction_count 9,172,416, duplicate_prediction_count 0, `prediction_artifact_written=false`, `prediction_artifact_write_skipped=true`, `prediction_path=null`, `output_root=null`, `predictions_root=null`, `artifact_evidence_ready=true`, active `data/predictions` files 0, and generated files under the full reports root are exactly the WFA report JSON and predictions manifest JSON. No provider calls, data replacement, commits, pushes, paper, or live work.
- Bounded full Phase 6 prediction-artifact materialization completed for the same accepted Tier 1 core scope and inputs as the full report-only PASS run. Command was `python -m scripts.phase6_wfa.run_wfa` with explicit `--write-predictions`, `--predictions-root data/predictions`, run `tier1_core_phase6_full_predictions_20260706`, reports root `reports/wfa/tier1_core_phase6_full_predictions_20260706`, split plan `reports/wfa/tier1_core_phase5_split_plan_20260706/split_plan.json`, feature set `reports/wfa/tier1_core_phase6_wfa_runner_preflight_20260706/tier1_core_active_feature_set.json`, and data-audit universe `reports/data_audit/wfa_research/tier1_rebuild_v1/preflight/data_audit_universe_tier1_rebuild_v1.json`. Evidence: `reports/wfa/tier1_core_phase6_full_predictions_20260706/tier1_core_phase6_full_predictions_20260706_wfa_report.json`, `_predictions_manifest.json`, and `data/predictions/tier1_core_phase6_full_predictions_20260706/oos_predictions.parquet`. Status `PASS`, failure_count 0, warning_count 0, fold_count 48, diagnostics 384 all PASS, 8 models, prediction_count 9,172,416, duplicate_prediction_count 0, `prediction_writes_enabled=true`, `prediction_artifact_written=true`, `prediction_artifact_write_skipped=false`, `prediction_path=data/predictions/tier1_core_phase6_full_predictions_20260706/oos_predictions.parquet`, `artifact_evidence_ready=true`, artifact evidence failures 0, stale output detected false, output hash matched the actual parquet SHA-256, parquet row count 9,172,416, missing required parquet columns 0, and `input_file_hashes` matched the full report-only PASS run. Generated repo artifacts are exactly the expected prediction parquet plus the WFA report JSON and predictions manifest JSON. No provider calls, data replacement, commits, pushes, paper, or live work.
- User accepted the fixed Phase 8 execution-realism warning as an expected research-only caveat with promotion/paper/live still blocked. Phase 8 reporting now separates fixed caveats from actionable warnings and implements the promotion-facing metric gate in `scripts/phase8_model_selection/evaluate_predictions.py`; targeted test `tests/phase8_model_selection/test_evaluate_predictions.py` asserts enriched metrics, no-trade baseline, missing-baseline blockers, cost-stress blockers, capacity/liquidity blockers, and gate propagation. The same bounded Phase 8 command reran for `tier1_core_phase6_full_predictions_20260706` with exact prediction parquet/manifest inputs, default `configs/costs.yaml` and `configs/models.yaml`, metrics root `reports/metrics/tier1_core_phase6_full_predictions_20260706`, model-selection root `reports/model_selection/tier1_core_phase6_full_predictions_20260706`, and phase8 root `reports/phase8/tier1_core_phase6_full_predictions_20260706`, without `--require-promotion-ready`. The rerun exited 0, generated exactly the expected 8 files, and satisfies strict stop conditions: failure_count 0, warning_count 0, warnings empty, expected file scope PASS, manifest/parquet hash match, final_holdout_touched false, research_policy_metrics_ready true, research_caveats present, policy rows 1,146,552, trades 1,347. Promotion metric gate is `FAIL` with 25 blockers. Current economics remain negative: gross dollars -19,871.875, net dollars -55,995.215, cost dollars 36,123.34, cost_drag_to_abs_gross 1.8178123604340164, avg_net_dollars_per_trade -41.57031551596151, profit_factor 0.6698645571410249, sortino_like -0.16843237188837107, calmar_like -0.9830233165845228, tail_loss_95_dollars -517.0, cvar_95_dollars -954.4255072463775, skew_net_dollars -2.120715008257902, kurtosis_net_dollars 15.882331056733557, net_sharpe_like -4.533891907460148. Blocking evidence includes no-trade not beaten, random-entry/simple futures-native baselines missing, 2x cost stress failed, capacity/liquidity/market-impact evidence missing, regime breakdown missing, and statistical-validity gate FAIL from missing PBO/Deflated Sharpe/Probabilistic Sharpe/bootstrap CI/multiple-testing/parameter-stability/regime-breakdown evidence. Treat this as warning-free diagnostic Phase 8 evidence, not alpha/promotion/paper/live readiness. No provider calls, data/prediction/model replacement, tuning, promotion, paper, live work, commits, or pushes ran.
- 2026-07-07 bounded read-only Tier 1 weak-alpha closeout completed for `tier1_core_phase6_full_predictions_20260706`. Disposition: `STOP_TIER1_WEAK_ALPHA`. Evidence reconciled from `CODEX_HANDOFF.md`, `PROJECT_OUTLINE.md`, `configs/costs.yaml`, Phase 6 WFA report/manifest, Phase 8 metrics/alpha decision, prediction audit, failure analysis, and statistical validity reports. Phase 6 prediction integrity remains structurally PASS, but Phase 8/failure-analysis economics are negative before and after costs: gross dollars -19,871.875, net dollars -55,995.215, costs 36,123.34, avg net/trade -41.57031551596151, profit factor 0.6698645571410249, and candidate does not beat no-trade. Later diagnostic reports create a paperwork/report-consumption mismatch because Phase 8 was generated before the later prediction audit/failure/statistical diagnostics, but that mismatch does not change the stop decision. Do not run further Tier 1 reruns, rescue variants, WFA/modeling, Phase 8 refreshes, tuning, promotion, artifact freeze, paper/live, or evidence refresh unless the user separately approves a paperwork-only consolidated evidence refresh; such a refresh must not be treated as alpha rescue.
- 2026-07-06 report-only stale cleanup disposition completed in `reports/data_audit/current_state/stale_cleanup_disposition_20260706.{json,md}`. No files were moved or deleted. The two actionable cleanup candidates are `data/_staging/tier1_core_phase34_20260706` (20 files, 979,413,035 bytes, archive-only candidate because original Phase 3/4 active placement was superseded by cleaned Phase 4 feature placement) and `data/_staging/tier1_core_phase4_self_reference_cleanup_20260706` (12 files, 816,287,231 bytes, delete/archive candidate only after a fresh hash precheck because current active placement evidence shows 12/12 source/destination hash matches). Protect `data/causally_gated_normalized_rebuild_staging/holdout_forward_8_phase2_staged_20260706` because the holdout/forward lane remains partial-pass/blocked with no active replacement. Keep `reports/features_baseline/active_root_baseline_features_20260705` as historical Phase 4 root-cause evidence; old small repo-level archive folders are deep-archive review candidates only. No provider calls, active data mutation, source/config/test edits, report pruning, WFA/model/prediction work, commit, push, paper, or live work ran.
- 2026-07-07 adversarial audit from `AUDIT.md` completed. Durable report: `docs/adversarial_futures_quant_system_audit_20260707.md`. Report-only model-trust input: `reports/model_trust_audit/codex_model_trust_audit_20260707T000000Z/model_trust_audit.{json,md}` with `permitted_use_label=research_only`. Audit verdict is `Fail`: overall score 34/100, research validity 42/100, trading-system readiness 34/100. Severe blockers are current Tier 1 costed OOS failure, baseline failure, statistical-validity failure, missing complete trial log, missing capacity/liquidity/market-impact evidence, and no production/paper/live acceptance evidence. No provider calls, data rebuilds, WFA reruns, prediction generation, tuning, cleanup, staging, commit, push, paper, or live work ran. Keep all discovery execution behind separate bounded approval of the exact command.
- 2026-07-07 ORAC v2 source/preflight/discovery packet, single approved discovery-run, and registry/trial-ledger disposition completed for `opening_range_acceptance_continuation_event_capture_30m_v2`. Config: `configs/alpha_discovery_generated/orac_v2_source_packet_20260707/alpha_discovery_runner.opening_range_acceptance_continuation_event_capture_30m_v2.json`; cost evidence: `docs/orac_v2_es_round_turn_cost_evidence_20260707.md` supports ES `round_turn_cost_dollars=29.50` / `round_turn_cost_ticks=2.36` with unchanged `configs/costs.yaml` and effective-date caveat `2026-06-14` vs CME source effective `2026-06-29`. One approved wrapper discovery-run generated expected ignored reports only: `reports/pipeline_audit/orac_v2_source_packet_20260707_opening_range_acceptance_continuation_event_capture_30m_v2_discovery_smoke.{json,md}`. Authoritative JSON decision `STOP_CLASS_COLLAPSE`, `candidate_passed=false`, `candidate_stopped=true`, `failure_count=0`, `top_total_net_dollars=-52.0`, `positive_top_net_fold_count=2`; Markdown confirms long/short/flat counts `237/225/0`. Registry now marks ORAC v2 `REJECTED`, `wfa_allowed=false`, source reports set to the generated MD/JSON, `next_allowed_actions=[]`; trial ledger has `phase9_discovery_smoke` rejection event `opening_range_acceptance_continuation_event_capture_30m_v2_discovery_smoke_rejected_20260707t153854z`. Validation after disposition: `python -m pytest -q tests\validation\test_orac_v2_trial_packet.py tests\validation\test_generate_alpha_discovery_candidates.py tests\validation\test_run_alpha_discovery.py` passed `70 passed`; `python -m scripts.validation.check_coordination_docs` passed; `git diff --check` passed with only existing CRLF warnings. No rerun, WFA/modeling, Phase 8, provider downloads, cost-config mutation, tuning, promotion, artifact freeze, paper/live, commit, or push ran.
- 2026-07-07 bounded source/test registration, single approved discovery-run, and registry/trial-ledger disposition completed for `session_compression_breakout_30m_v1`. Source implements an ES-only 30-minute fixed-horizon target using a completed prior 30-bar same-session compression box, causal prior 120-box rolling 25th percentile threshold with 60-box minimum, one-tick breakout buffer, next-open entry, fixed 30-minute timeout exit, unchanged costs, and non-overlap before fold scoring. Guarded config is `configs/alpha_discovery_generated/session_compression_source_packet_20260707/alpha_discovery_runner.session_compression_breakout_30m_v1.json`. Validation before discovery passed: direct source-test command `117 passed`; wrapper preflight `PREFLIGHT_PASS` with 0 present expected outputs; wrapper source-tests `SOURCE_TESTS_PASS`; wrapper discovery-packet `DISCOVERY_PACKET_READY`. One approved wrapper discovery-run generated expected ignored reports only: `reports/pipeline_audit/session_compression_source_packet_20260707_session_compression_breakout_30m_v1_discovery_smoke.{json,md}`. Authoritative JSON decision `STOP_CLASS_COLLAPSE`, `candidate_passed=false`, `candidate_stopped=true`, `failure_count=0`, `top_total_net_dollars=-1540.5`, `positive_top_net_fold_count=3`, event counts long/short/flat `514/505/5985`, duplicate overlap `0.7723258096172718 <= 0.8 cap`, and `target_smoke_is_tradability_proof=false`; Markdown confirms the same stop evidence and Do Not Do guidance. Registry now marks the branch `REJECTED`, `wfa_allowed=false`, source reports set to the generated MD/JSON, and `next_allowed_actions=[]`; trial ledger has rejection event `session_compression_breakout_30m_v1_discovery_smoke_rejected_20260707t220658z`. Do not rerun discovery, tune thresholds/features/costs/folds, run WFA/modeling, Phase 8, provider downloads, cost-config mutation, promotion, artifact freeze, paper/live, commit, or push.
- 2026-07-07 bounded single discovery smoke completed for `late_session_range_resolve_session_close_v1` after prior source-test and discovery-packet readiness. Command: `python -m scripts.validation.run_alpha_discovery --config configs\alpha_discovery_generated\late_session_source_packet_20260707\alpha_discovery_runner.late_session_range_resolve_session_close_v1.json --mode discovery-run --approve-discovery-run --approval-token RUN_PHASE9_DISCOVERY_ONCE`. Runner status was `DISCOVERY_RUN_CANDIDATE_STOPPED`; generated ignored reports are `reports/pipeline_audit/late_session_source_packet_20260707_late_session_range_resolve_session_close_v1_discovery_smoke.{json,md}`. Authoritative JSON decision is `STOP_CLASS_COLLAPSE`, `candidate_passed=false`, `candidate_stopped=true`, `failure_count=0`, `event_count=0`, `fold_count=0`, `top_total_net_dollars=0.0`, `positive_top_net_fold_count=0`, and `target_smoke_is_tradability_proof=false`; gates failed class balance, duplicate overlap availability, fold stability, and positive stage net. Markdown cross-check matches the JSON and includes Do Not Do guidance. Registry/ledger disposition now marks the branch `REJECTED`, keeps `wfa_allowed=false`, attaches the generated reports, clears `next_allowed_actions`, and appends one rejected trial-status row. Do not rerun discovery, tune thresholds/features/costs/folds/session windows, run WFA/modeling, Phase 8, provider downloads, promotion, artifact freeze, paper/live, commit, or push from this branch.
- 2026-07-09 paused-modeling evidence gate and terminal closeout implemented. Report-only commands: `python -m scripts.validation.build_alpha_evidence_gap_matrix` and `python -m scripts.validation.build_alpha_evidence_completion_closeout`; tests: `tests/validation/test_build_alpha_evidence_gap_matrix.py` and `tests/validation/test_build_alpha_evidence_completion_closeout.py`; outline entries added under Validation Commands. Fixed matrix `reports/model_trust_audit/alpha_evidence_gap_matrix_20260709T034313Z/alpha_evidence_gap_matrix.json` has verdict `PAUSE_MODELING_BASELINE_NULL_EXECUTION_EVIDENCE_INCOMPLETE`, `alpha_evidence_ready=false`, counts `PASS=6`, `FAIL=6`, `MISSING_EVIDENCE=11`, and 17 blockers. Terminal closeout report: `reports/model_trust_audit/alpha_evidence_completion_closeout_20260709T035929Z/alpha_evidence_completion_closeout.json` with verdict `CLOSE_CURRENT_LINE_NO_ALPHA_EVIDENCE`, `modeling_pause_required=true`, `future_modeling_allowed=false`, `future_evidence_work_allowed=true`, `promotion_allowed=false`, classifications `terminal_fail=5`, `missing_required_evidence=11`, `diagnostic_pass_only=6`, `not_actionable_for_current_line=1`. Current Tier 1 line is closed for alpha evidence; this does not approve target discovery, WFA/modeling, Phase 8 refresh, promotion, artifact freeze, final holdout, paper/live, provider/download commands, cleanup, staging, commit, push, or rescue tuning.
- 2026-07-09 adversarial current-evidence gate audit completed in `docs/adversarial_current_project_evidence_gate_audit_20260709.md`. Audit verdict is `Fail / Blocked for trading and alpha acceptance`; overall adversarial score is 22/100; current Tier 1 remains closed for alpha evidence. The audit used local primary evidence only: July 9 alpha evidence matrix/closeout, Phase 8 promotion decision, failure analysis, statistical-validity summary, costs config, target registry/trial statuses, `PROJECT_OUTLINE.md`, prior audit docs, and targeted `live_ops` scaffold inspection. It changed no public API, schema, config, registry, ledger, data, model, quant logic, generated reports, staging, commit, push, provider, target discovery, source test, WFA/modeling, Phase 8 refresh, artifact freeze, final holdout, paper, or live paths. Validation: `python -m scripts.validation.check_coordination_docs` and `git diff --check` were run after doc/handoff edits.
# Known Issues
- The old broad `all_raw` alignment PASS used an older repair manifest for SR1/SR3 2020 whose `source_file` entries named `data/dbn_sr_parent_candidate`. The refreshed post-SR alignment manifest now points accepted SR repair sources to `data/dbn/ohlcv_1m_parent`. Remaining caveat: generic `audit_raw_dbn_alignment` does not independently validate SR status/statistics sidecar source hashes; use `reports/data_audit/sr1_sr3_2020_parent_sidecar_raw_replacement_20260705.json` as the primary sidecar-lineage proof.
- Latest active-root review after the user-approved historical 6M/TN exclusions shows the six stale raw-input causal rows resolved and the four old historical rows excluded without raw deletion; remaining active raw-without-causal rows are only `6E`, `CL`, `ES`, and `ZN` 2025/2026, 2025 has staged-only PASS evidence with no active replacement approval, 2026 has ES/NQ degraded-session diagnostic evidence but no approved exception/rebuild, local-trade/OHLCV gap gate remains `NOT_RUN`, historical/candidate causal evidence roots remain provenance labels, pre-reset broad label/feature evidence is historical, and active v2 label/feature/split/preflight/report-only WFA evidence is limited to the eight Tier 1 core files above and not downstream trading, promotion, paper/live, or model-readiness evidence.
# Next Steps
Exact next recommended step: Plan a read-only final status handoff and workspace publication decision using `reports/master_audit/master_audit_terminal_closeout_refresh_20260710/master_audit_terminal_closeout_refresh.{json,md}` and current `git status --short`; target only explaining what is legitimately closed/blocked, what remains dirty/unpublished, and whether to leave the workspace as-is or request separate staging/commit approval. Do not mutate ledgers, registry, trial statuses, reports, data/model artifacts, run generated report execution, WFA/modeling, predictions, Phase 8 refresh, provider/network calls, promotion, artifact freeze, final holdout, paper/live, cleanup, staging, commit, or push without separate approval.
