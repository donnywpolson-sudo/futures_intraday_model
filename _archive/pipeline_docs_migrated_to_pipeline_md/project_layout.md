# Project Layout

`PIPELINE.md` is the authoritative project layout, pipeline contract, phase
specification, runbook, and current-status appendix.

This file is intentionally a pointer stub to avoid maintaining a second,
divergent architecture document. See `PIPELINE.md` for:

- stable data and artifact paths
- profile and holdout policy
- phase-by-phase inputs, commands, outputs, acceptance checks, and stop
  conditions
- validation commands
- current Tier 1 evidence and next valid work
